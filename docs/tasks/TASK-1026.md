## TASK-1026: enforcement boundary demo moment in arch init walkthrough
**Meta:** P0 | S | READY | Focus:yes | 2-code-generation | local | cli/src/main/ts/application/commands/init-command.ts

**Depends:** none

### Context

The marketing guarantee — "ARCH is the only tool that guarantees your AI cannot corrupt your project state, because the AI literally cannot enforce its own suggestions" — is a claim, not evidence, unless the user sees the block happen. The current `arch init` flow demonstrates detection (review findings) but not enforcement separation. This task adds a concrete enforcement moment to the init walkthrough: a simulated (or real, if detectable) governance violation that is blocked by the pre-commit hook, with a plain-language explanation naming the mechanism. Source: IDEA-enforcement-boundary-demo.

### Acceptance Criteria

- [ ] During `arch init` walkthrough, stage or instrument a governance violation attempt (e.g., a commit missing a TASK-ID reference).
  - `file: cli/src/main/ts/application/commands/init-command.ts`

- [ ] The pre-commit hook fires and blocks the violation with a plain-language explanation that includes the phrase "blocked by `arch review`" (or equivalent naming the mechanism, not asking the AI to stop).
  - `prose: running the demo moment shows hook output naming the enforcement mechanism explicitly`

- [ ] If an existing AI agent integration is detected in the repo during the init scan, substitute the simulation with a real violation from that repo's history.
  - `prose: on a repo with detectable AI agent config, the demo uses a real violation, not a placeholder`

- [ ] If no AI agent integration is detected, the simulation uses a clearly-labeled synthetic example (not presented as real).
  - `prose: on a fresh repo, the demo is labeled "simulated" or equivalent — no false impression of a real violation`

- [ ] The enforcement moment is the last step of the init walkthrough, after review findings are surfaced.
  - `prose: init walkthrough order: scan → bootstrap .arch/ → review findings → enforcement demo`

- [ ] `npm test` passes.
  - `prose: all existing tests pass`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
