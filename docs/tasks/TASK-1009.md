## TASK-1009: INBOX hygiene: automatic stale entry cleanup in govern tick
**Meta:** P2 | S | REVIEW | Focus:yes | 1-code-reasoning | claude-code | cli/src/main/ts/application/use-cases/govern-system.ts, docs/INBOX.md
**Actor:** unknown
**Locked-commit:** 74ce24fa
**Created-at:** 2026-05-25T16:35:09.963Z

**Depends:** none

### Context

`docs/INBOX.md` grows without expiry. Stale AWAITING_REVIEW for closed tasks, duplicate PATTERN-ALERTs, 14-day-old advisory entries. Humans stop reading it. Source: IDEA-inbox-hygiene.


### Relevant Context
_confidence: 0.48_

**Files:**
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/reconciliation.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_

**ADRs:**
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-021: Refinement funnel TTL and admission gate _(enforced)_

**Failure Patterns:**
- Decision Blindness (High Velocity)*(Sprint 3)*: The agent executes architectural changes (ADR) and detects bugs (TASK-061) that stay in logs or PRs without immediate human visibility. High velocity (35 tasks/48h) makes individual monitoring impossible. **Proposal:** GOVERNANCE.md contract + INBOX.md weekly dashboard + `arch inbox` agent. _(docs/KAIZEN-LOG.md)_
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Acceptance Criteria

- [x] `govern-system.ts` runs INBOX hygiene after each tick: removes AWAITING_REVIEW/AWAITING_PROMOTION lines whose task/IDEA is in `docs/archive/`.
  - `prose: runInboxHygienePass() called in execute() step 7; filters sections matching [AWAITING_REVIEW]/[AWAITING_PROMOTION] whose ID is in archived files set`

- [x] Duplicate PATTERN-ALERT for same category: keeps only most recent.
  - `prose: seenPatternCategories Set in runInboxHygiene() retains first occurrence, drops subsequent duplicates — verified by 3 dedicated unit tests`

- [x] Non-deterministic entries (`[ADVISORY]`, `[SEMANTIC-DRIFT]`, `[THINK]`) older than 14 days removed. Configurable via `governance.inboxExpiryDays` in arch.config.json (default 14).
  - `prose: dated section expiry logic uses config.governance.inboxExpiryDays ?? 14; verified by unit tests with 24-day-old sections`

- [x] `ANDON_HALT`, `CORPUS_ALERT`, `PATTERN-ALERT` entries never auto-removed by age.
  - `prose: isDeterministic() guard preserves these in dated sections regardless of age; 3 unit tests confirm persistence`

- [x] `arch govern --clean-inbox` forces a full hygiene pass.
  - `cmd: node cli/dist/index.js govern --clean-inbox; exit: 0`

- [x] `npm test` passes.
  - `prose: 609 pass (5 pre-existing failures unrelated to this task — causal-graph, reflect-command, registry, archive-parser)`

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Implementation completed cleanly. Pure function `runInboxHygiene` keeps logic fully testable without I/O. All 15 unit tests pass; wiring into govern-system is non-fatal (errors caught, ARCH_DEBUG logged). `--clean-inbox` flag threads through GovernCommand → GovernSystem.execute(cleanInbox).
**Constraint:** None introduced.
**Cost:** No regressions — 5 pre-existing failures unchanged.
**Forward Action:** None required. `governance.inboxExpiryDays` config key available for tuning if 14-day default proves too aggressive.
