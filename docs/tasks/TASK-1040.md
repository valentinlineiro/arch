## TASK-1040: add c8 coverage reporting to npm test
**Meta:** P1 | XS | READY | Focus:yes | 3-chore | local | cli/package.json

**Depends:** TASK-1039

### Context

No coverage measurement exists. Actual branch coverage is unknown — "35% source-to-test ratio" is a line-count proxy, not a coverage number. c8 wraps the existing node:test runner with zero configuration changes to test files. TASK-1039 must complete first so coverage runs against a green suite — measuring coverage against a broken suite produces misleading baselines.

### Acceptance Criteria

- [ ] `c8` is added as a dev dependency in `cli/package.json`.
  - `file: cli/package.json`

- [ ] `"test:coverage": "c8 node --import tsx --test src/test/ts/*.test.ts"` is added to scripts.
  - `file: cli/package.json`
  - `cmd: npm run test:coverage` produces a coverage report without errors

- [ ] Coverage report output includes per-file line, branch, and function coverage percentages.
  - `prose: npm run test:coverage output shows a coverage table with branch % column`

- [ ] Baseline coverage numbers for the three largest source files (drift-checker.ts, govern-system.ts, command-dispatcher.ts) are recorded in the task's Hansei section before closing.
  - `prose: Hansei Decision field contains baseline branch coverage % for each of the three files`

- [ ] `npm test` (without c8) still works unchanged.
  - `cmd: npm test` exits 0

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
