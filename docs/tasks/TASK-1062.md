## TASK-1062: ARCH CD pipeline: sprint-close tagging, GitHub Actions publish, consumer upgrade
**Meta:** P2 | M | READY | Focus:no | 2-code-generation | local | .github/workflows/, cli/src/main/ts/application/use-cases/govern-system.ts, cli/src/main/ts/application/commands/
**Actor:** unknown
**Created-at:** 2026-05-27T12:30:00.000Z

**Depends:** TASK-1055

<!-- prereq: configure npm trusted publisher before first tag push:
     npm → package settings → Publishing → Trusted Publishers → GitHub Actions
     owner: valentinlineiro  repo: arch  workflow: release.yml
     No NPM_TOKEN needed — OIDC token from the workflow authenticates.
     Phases 1+2 (tagging + publish workflow) are independent of TASK-1055; Phase 3 (arch upgrade) requires it. -->

### Context

Promoted from IDEA-arch-release-and-cd.

ARCH is published to npm manually today. This task implements the full CD loop:

1. **Sprint close → auto-tag**: `govern-system.ts` sprint-close path bumps the patch version in `cli/package.json` + `arch.config.json`, creates a git tag `vX.Y.Z`, and pushes it.
2. **Tag → publish**: new `.github/workflows/release.yml` triggers on `v*.*.*` tags — builds, tests, publishes to npm, creates a GitHub Release with a `feat:`/`fix:` changelog.
3. **Consumer upgrade**: `arch upgrade` (TASK-1055) uses the published version as its source.

Version bump strategy: patch on every sprint close. Minor/major requires `nextVersionBump: minor|major` in `arch.config.json` (govern reads it, applies it once, resets to `patch`).

### Gaps

Requires human confirmation before starting:
- `NPM_TOKEN` secret set in repo settings
- Confirm patch-on-close is the desired default (not minor)

### Acceptance Criteria

- [x] `.github/workflows/release.yml` exists and triggers on `v*.*.*` tag push. Jobs: build → test → npm publish → gh release create. Uses OIDC trusted publisher — no NPM_TOKEN. → file: .github/workflows/release.yml
- [ ] Changelog in the GitHub Release lists `feat:` and `fix:` commits since the previous tag. → prose: release notes verified after first tag push
- [ ] `govern-system.ts` sprint-close path bumps patch version in `cli/package.json` and `arch.config.json`. → file: cli/src/main/ts/application/use-cases/govern-system.ts
- [ ] Sprint close creates a git tag `vX.Y.Z` and pushes it. → prose: unit test verifies tag creation call on sprint close; integration: tag visible in repo after close
- [ ] `nextVersionBump` field in `arch.config.json` allows one-shot minor/major bump; govern resets it to `patch` after use. → prose: unit test: config has nextVersionBump:minor, close produces vX.(Y+1).0, config reset to patch
- [ ] `arch upgrade` successfully upgrades an outdated install to the latest published version. → prose: verified via TASK-1055 implementation
- [ ] `npm test` passes. → cmd: npm --prefix cli test; exit: 0
- [ ] `arch review` passes. → cmd: node cli/dist/index.js review; exit: 0

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
