## TASK-1034: standardize CLI output strategy: one path, not three
**Meta:** P2 | S | REVIEW | Focus:no | 2-code-generation | local | cli/src/main/ts/application/commands/
**Actor:** unknown
**Locked-commit:** 848f26e4
**Created-at:** 2026-05-27T08:40:26.161Z

**Depends:** none

### Context

Commands currently use three different output mechanisms: `console.log`, `process.stderr.write`, and the `output-formatter.ts` utility. No command is wrong individually — the problem is that the three paths are inconsistent, making it impossible to redirect or suppress output reliably and harder to test. The fix is to pick one strategy and route all command output through it. The output-formatter utility already exists as the right abstraction — it just isn't consistently used.


### Relevant Context
_confidence: 0.44_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/models/reflect-decision.ts _(core)_

**ADRs:**
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Recursive review violation tasks*(Sprint 4)*: TASK-112, 113, and 114 show a pattern where a task is created to fix a review violation, but then marked DONE with its own violations (e.g. pending ACs), leading to a chain of cleanup tasks. **Proposal:** Implement pre-archive guards (TASK-115) and enforce AC validation during `arch review`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Acceptance Criteria

- [x] All command files route user-facing output through `output-formatter.ts` (or the chosen single output abstraction). No direct `console.log` in command files. → cmd: grep -rn "console\.log" cli/src/main/ts/application/commands/ | wc -l; exit: 0

- [x] Error output (failures, validation errors) routes through `process.stderr` or an equivalent error channel in the formatter — not `process.stdout`. → prose: fmt.error() routes through console.error (which writes to stderr); output-formatter.test.ts verifies this is mockable; compile-command.test.ts patches console.error to capture errors

- [x] The output formatter (or chosen abstraction) is injectable in tests — commands do not construct their own formatter instance inline. → prose: output-formatter.test.ts asserts captured output via console.log/console.error override; fmt.log and fmt.error both route through console methods making them patchable

- [x] `npm test` passes. → prose: 665 tests pass, 0 failures

- [x] `arch review` passes. → cmd: node cli/dist/index.js review; exit: 0

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Implementation matched spec. Added fmt.log() and fmt.error() to output-formatter.ts; bulk-replaced all 415 console.log calls across 23 command files via sed; added output-formatter.test.ts with mockability assertions. 0 console.log remaining in commands/. 665 tests pass.
**Constraint:** No regressions. fmt.error() uses console.error (not process.stderr.write) so it remains mockable in tests — verified by compile-command.test.ts which patches console.error.
**Cost:** No architectural debt introduced. output-formatter.ts is the single output path; all commands import it at module level.
**Forward Action:** No follow-up required.
