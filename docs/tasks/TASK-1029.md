## TASK-1029: structural compaction: stale files, sprawl invariant, escalation compaction, archive partitioning
**Meta:** P1 | M | READY | Focus:yes | 3-chore | local | .arch/, docs/, cli/src/main/ts/domain/services/drift-checker.ts

**Depends:** none

### Context

The repo structure exhibits design-by-accretion: correct buckets (.arch/, docs/, cli/), wrong nesting discipline. 26 flat files in .arch/, 22 standalone files in docs/, stale artifacts in version control, and no enforcement ceiling. This task addresses the items with clear operational urgency or that prevent recurrence. docs/ restructuring is explicitly out of scope during the 90-day sprint (path coupling risk; see TASK-1018 blast radius). Items 5 and beyond (monorepo split, docs/ reorganization) are deferred to Phase 2 and covered by TASK-206 and existing ROADMAP entries.

### Acceptance Criteria

- [ ] Delete stale artifacts: `docs/index.html.bak` and any `.think-prompt-*.md` files in `.arch/`.
  - `cmd: git ls-files docs/index.html.bak .arch/.think-prompt-*.md | wc -l` should return 0 after deletion

- [ ] Add a `maxTopLevelFiles` check to `arch review` (DriftChecker): fail if any of `.arch/`, `docs/`, or root have more top-level entries than a configurable threshold (defaults: `.arch/` = 30, `docs/` = 25, root = 25). Threshold must be readable from `arch.config.json`.
  - `file: cli/src/main/ts/domain/services/drift-checker.ts`
  - `prose: arch check reports a violation when a test directory exceeds the threshold`

- [ ] Add escalation compaction to `arch govern`: on each govern tick, write a compacted view of `.arch/escalations.jsonl` to `.arch/escalations-compacted.jsonl` containing only OPEN records. Original file remains append-only and is never mutated. The compacted view is the read path for all govern logic that queries escalation state.
  - `file: cli/src/main/ts/application/commands/govern-command.ts`
  - `prose: after running arch govern on a repo with >50 resolved escalation records, escalations-compacted.jsonl contains only OPEN records`

- [ ] File TASK-1030 as a READY task scoped to archive partitioning strategy (year/sprint subdirectories in docs/archive/) before closing this task, with a trigger condition: promote when docs/archive/ exceeds 500 files.
  - `prose: TASK-1030 exists in docs/tasks/ with READY status and a 500-file trigger condition in its Context`

- [ ] Merge the duplicate tension templates: keep `docs/tensions/TEMPLATE.md`, delete `docs/tensions/TENSION-template.md`. Verify no task references the deleted file.
  - `cmd: git ls-files docs/tensions/TENSION-template.md | wc -l` should return 0

- [ ] Add ADRs to the context budget in `arch.config.json` so `arch memory` and context indexing include them.
  - `file: arch.config.json`
  - `prose: arch memory ask "what does ADR-026 say" returns content from docs/adr/ADR-026.md`

- [ ] `npm test` passes.
  - `prose: all existing tests pass`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
