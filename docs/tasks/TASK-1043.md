## TASK-1043: consolidate mocks and add makeBaseFs() fixture factory
**Meta:** P2 | M | IN_PROGRESS | Focus:yes | 2-code-generation | local | cli/src/test/ts/mocks/index.ts
**Actor:** unknown
**Locked-commit:** 80261e33
**Created-at:** 2026-05-28T06:15:27.251Z

**Depends:** TASK-1042

### Context

`govern-system.test.ts` defines its own SpyFileSystem and SpyGitRepository that are functionally identical to MockFileSystem/MockGitRepository in mocks/index.ts, differing only in that SpyFileSystem records `writeCalls` while MockFileSystem records `written`. Multiple other test files define inline mock classes (e.g. MockFs in project-command.test.ts). The result: 3+ implementations of the same mock, diverging under maintenance. The fix: add `writeCalls` tracking to the shared MockFileSystem, delete per-file duplicates, and add a `createTestRepo()` factory function so test setup is one function call rather than 10 lines of inline construction. TASK-1042 (typed mocks) first — consolidation is easier once all mocks are typed.


### Relevant Context
_confidence: 0.45_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/models/reflect-decision.ts _(core)_

**ADRs:**
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_
- ADR-017: Deterministic Observability & Operational Metrics _(enforced)_
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Recursive review violation tasks*(Sprint 4)*: TASK-112, 113, and 114 show a pattern where a task is created to fix a review violation, but then marked DONE with its own violations (e.g. pending ACs), leading to a chain of cleanup tasks. **Proposal:** Implement pre-archive guards (TASK-115) and enforce AC validation during `arch review`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Gaps

None identified — scope is bounded to test-file consolidation with clear before/after verification commands.

### Acceptance Criteria

- [ ] `MockFileSystem` in `mocks/index.ts` gains a `writeCalls` array tracking each `write()` invocation (path + content).
  - `file: cli/src/test/ts/mocks/index.ts`

- [ ] `SpyFileSystem` and `SpyGitRepository` in `govern-system.test.ts` are deleted; the file uses the shared mocks instead.
  - `cmd: grep -n "SpyFileSystem\|SpyGitRepository" cli/src/test/ts/govern-system.test.ts | wc -l` returns 0

- [ ] Inline mock classes in other test files (e.g. `MockFs` in project-command.test.ts) are replaced with the shared mocks.
  - `cmd: grep -rn "class Mock" cli/src/test/ts/ | grep -v "mocks/index" | wc -l` returns 0

- [ ] A `createTestRepo(options?)` factory is exported from `mocks/index.ts` that returns a pre-configured `{ fs, git }` pair with sensible defaults (empty .arch/ dir, docs/tasks/ dir present).
  - `file: cli/src/test/ts/mocks/index.ts`
  - `prose: at least 3 test files updated to use createTestRepo() instead of inline mock construction`

- [ ] `npm test` passes.
  - `cmd: npm test` exits 0

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
