## TASK-1078: ARCH release and CD pipeline: automated version bump + npm publish
**Meta:** P2 | M | READY | Focus:no | 2-code-generation | claude-code | .github/workflows/, cli/package.json

**Depends:** none

### Gaps
Release process is fully manual. No CI trigger, no changelog generation, no publish gate.

### Context

Source: IDEA-arch-release-and-cd.md

Publishing a new ARCH version requires: bump cli/package.json manually, write changelog manually, run npm publish manually, tag the release manually. Zero automation. The OSS adoption path requires a reliable, low-friction release process. With the backlog clear this is the next strategic investment.

### Acceptance Criteria

- [ ] `.github/workflows/release.yml` exists and triggers on push to main when `cli/package.json` version changes.
  - `file: .github/workflows/release.yml`

- [ ] Workflow runs `npm test` and exits non-zero on failure: no publish on broken tests.
  - `prose: workflow has a test step before publish that blocks on failure`

- [ ] Workflow runs `npm publish --access public` for the `cli/` package.
  - `prose: workflow publishes to npm on version bump`

- [ ] Workflow creates a GitHub release with a generated changelog (commits since last tag).
  - `prose: GitHub release created with changelog on version bump`

- [ ] `arch upgrade` successfully detects the new version from npm.
  - `prose: arch upgrade reports new version after publish`

- [ ] `npm test` passes locally.
  - `prose: 707+ tests pass`

### Definition of Done
- [ ] All ACs checked

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
