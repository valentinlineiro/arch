## TASK-1067: codebase-aware context injection from declared task paths
**Meta:** P2 | M | READY | Focus:no | 2-code-generation | local | cli/src/main/ts/application/use-cases/

### Context

`arch task start TASK-XXX` injects context by querying the corpus for semantically similar past tasks. When the corpus contains only governance artifacts (new project, first sprint), every query returns 0.00 confidence. The human receives no useful context.

The task meta line already contains a context path (e.g. `**Meta:** P1 | M | READY | Focus:no | 2-code-generation | claude-code | src/server/routes/`). This path is used today to scope `git log` evidence but is not used to answer: "what are the most-changed functions in this directory recently, and what do those files look like?"

Extend `arch task start` context injection with a codebase phase that runs before (or instead of, when corpus confidence is low) the corpus query: identify candidate files from the context path, surface recently changed functions via git log/diff, and inject as `[CODEBASE-CONTEXT]` suggestion.

### Gaps

None identified — scope is bounded to context injection extension with clear heuristics and fallback behavior.

### Acceptance Criteria

- [ ] `arch task start` on a task with declared context path surfaces recently changed files in that path  →  prose: verified by starting a task with a context path containing recently modified files
- [ ] Output includes `[CODEBASE-CONTEXT]` block when corpus confidence is below threshold  →  prose: verified on a new project with empty corpus
- [ ] Codebase context is appended (not primary) when corpus confidence ≥ 0.3  →  prose: verified on a project with populated corpus
- [ ] Context injection completes in under 3 seconds on a repo with 500+ files  →  prose: verified by timing arch task start on the cli/ subdirectory
- [ ] All existing tests pass  →  cmd: npm test --prefix cli; exit: 0
- [ ] `arch review` passes  →  cmd: arch review; exit: 0

### Definition of Done
- [ ] All ACs checked  →  prose: all predicates above verified
- [ ] `arch review` passes  →  cmd: arch review; exit: 0

## Hansei
**Severity:** H0
**Category:** [DeferredTest]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
