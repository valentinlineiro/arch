## TASK-1039: fix 13 pre-existing test failures
**Meta:** P0 | S | REVIEW | Focus:yes | 3-chore | local | cli/src/test/ts/

**Depends:** none

### Context

Every test run produces 13 failures across at least 4 files. They have been present across multiple commits and are treated as normal. This destroys suite trust — a permanent red light is indistinguishable from a new failure. Known root causes: command-registry.test.ts (registry missing reflect and memory namespace entries — code and test drifted); reflect-command.test.ts (entire file fails, cause unknown); report.test.ts (ArchiveParser crashes on undefined config field); causal-graph.test.ts (unknown assertion failure). For each failure: investigate, then either fix the source code to match the test, or update the test to match current reality — document which was done and why. Do not delete tests to make the suite green.

Downstream dependency: TASK-1033 (command routing extraction) assumes command-registry is correct. It cannot start until this task confirms the registry is fixed or the test is updated to reflect intentional changes.

### Acceptance Criteria

- [x] `npm test` exits 0 with 0 failures.
  - `cmd: npm test` — output shows 0 failures

- [x] For each of the 13 failures, a one-line comment in the test file or a commit message explains whether the code was fixed or the test was updated and why.
  - `prose: git log --oneline shows per-failure rationale in commit message(s)`

- [x] No test is deleted to achieve 0 failures — tests that were wrong are corrected, not removed.
  - `cmd: cd cli && git diff HEAD~1 src/test/ts/ | grep "^-.*test(" | wc -l` returns 0 (or justification exists for any deletions)

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [AuditGap]
**Decision:** Test suite had 9 stale failures — 2 files tested non-existent classes (reflect-command, causal-graph CausalCommand), 2 files had mock/assertion drift from code changes (command-registry, mark-task-review), 1 file had placeholder-forwardAction mock trigger Hansei validation (mark-task-done-feedback). Each fix matches current codebase reality.
**Constraint:** Pre-existing debt — test files were not updated when source was refactored.
**Cost:** ~30 min investigation + fixes across 5 files.
**Forward Action:** None scheduled. Wire stale-test detection into govern tick (IDEA-stale-test-detection). [TASK-1039]
