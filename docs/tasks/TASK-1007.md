## TASK-1007: arch project init: decompose spec into ADRs and task graph via LLM
**Meta:** P1 | M | REVIEW | Focus:yes | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/project-command.ts, cli/src/main/ts/application/command-dispatcher.ts
**Actor:** unknown
**Locked-commit:** 712804c7
**Created-at:** 2026-05-25T13:45:11.119Z

**Depends:** none

### Context

Building a project with ARCH requires manually writing the first ADR and decomposing the spec into tasks. This bottleneck prevents autonomous project initialization. Source: IDEA-project-scaffold. Advisory only — LLM proposes, human ratifies before execution.


### Relevant Context
_confidence: 0.44_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/audit-inference.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_

**ADRs:**
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-017: Deterministic Observability & Operational Metrics _(enforced)_

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Unstructured IDEAs before TASK-033*(Sprint 3)*: The original TEMPLATE only had `## Proposal` with no structured fields. THINK had to infer gaps without dependency or size context. The first 8 IDEAs were refined with more friction than necessary. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Gaps
- `project-command.ts` does not exist — must be created from scratch
- No `case 'project'` in `command-dispatcher.ts` — must be wired
- LLM call pattern: use `ProviderRegistry` + `BridgeProvider.complete()` to capture JSON output
- AC predicate format uses sub-line backtick style — must be rewritten to inline `→` format before `arch task review`

### Acceptance Criteria

- [x] `arch project init` command exists and is registered in dispatcher. → file: cli/src/main/ts/application/commands/project-command.ts

- [x] Command calls the configured LLM with a structured decomposition prompt outputting JSON with adrs/tasks/dod arrays. → prose: verified by ProjectCommand tests — prompt content and JSON schema validated

- [x] Generated ADRs written to docs/adr/ADR-NNN-slug.md; tasks to docs/tasks/TASK-NNN.md with standard ARCH meta format. → prose: verified by ProjectCommand tests — file paths and content structure validated

- [x] docs/PROJECT.md created with goal, scope, DoD predicates, and Ratification section. → prose: verified by ProjectCommand test — Ratification and Definition of Done sections present

- [x] Malformed JSON from LLM causes non-zero exit and no partial files written. → prose: verified by ProjectCommand test — rejects malformed JSON with no ADR/task files created

- [x] --depth N flag controls decomposition depth (default 2). → prose: verified by ProjectCommand tests — depth 2 default and --depth 3 override both tested

- [x] npm test passes for project-command tests. → cmd: sh -c 'cd /home/valentin/code/arch/cli && node --import tsx --test src/test/ts/project-command.test.ts'; exit: 0

- [x] arch review passes. → cmd: node /home/valentin/code/arch/cli/dist/index.js review; exit: 0

### Definition of Done
- [x] All ACs checked → prose: all ACs verified above
- [x] arch review passes → cmd: node /home/valentin/code/arch/cli/dist/index.js review; exit: 0

## Hansei
**Severity:** H0
**Category:** [AuditGap]
**Decision:** Implementation matched spec. ProjectCommand written test-first with 9 passing tests. Command wired into dispatcher and TypeScript build is clean. No architectural compromises made.
**Constraint:** No blocking constraints encountered during implementation.
**Cost:** No technical debt introduced by this implementation.
**Forward Action:** None required. Task closed cleanly.
