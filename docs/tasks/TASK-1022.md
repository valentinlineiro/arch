## TASK-1022: Progressive CLI surface: two-tier help visibility
**Meta:** P0 | S | IN_PROGRESS | Focus:no | 2-code-generation | local | cli/src/main/ts/application/command-dispatcher.ts, cli/src/main/ts/domain/services/command-registry.ts
**Actor:** unknown
**Created-at:** 2026-05-25T17:25:21.237Z
**Depends:** none

### Acceptance Criteria

- [ ] `arch help` (default, no flags) displays exactly three commands: `init`, `review`, `task capture`, with one-line descriptions each.
  - `prose: arch help output contains only init, review, task capture — no other commands listed`

- [ ] `arch help --full` displays all available commands in the existing format.
  - `prose: arch help --full shows the same command inventory as the current full help`

- [ ] All commands not in the default surface remain fully callable (e.g., `arch govern` works even though it is not listed in default help).
  - `prose: arch govern runs correctly after this change; commands are not removed, only hidden from default help`

- [ ] The default help output includes a pointer to `arch help --full`.
  - `prose: arch help output includes a line like "Run arch help --full to see all commands"`

- [ ] No tier-unlock state is introduced: no usage counters, no flags written to `.arch/` config, no progressive reveal logic.
  - `file: cli/src/main/ts/application/command-dispatcher.ts`

- [ ] `npm test` passes.
  - `prose: all existing tests pass`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Context

Restructures the help surface into a two-tier visibility gate: `arch help` shows three commands (init, review, task capture); `arch help --full` shows the full inventory. No state machine, no usage tracking. All commands remain callable at all times — only the help surface is gated. Scope decision: the 3-tier progressive unlock (original intent) is replaced by this simpler 2-tier gate per IDEA-progressive-cli-surface Decision.

### Relevant Context
_confidence: 0.45_

**Files:**
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_

**ADRs:**
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-013: Two-Tier Drift Detection Architecture _(enforced)_
- ADR-014: Causal Graph Schema for Chronicle _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Unstructured IDEAs before TASK-033*(Sprint 3)*: The original TEMPLATE only had `## Proposal` with no structured fields. THINK had to infer gaps without dependency or size context. The first 8 IDEAs were refined with more friction than necessary. _(docs/KAIZEN-LOG.md)_
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

#### Intent
capture Progressive CLI surface: restructure help into 3 tiers — Day 1 shows only init/review/task capture; Tier 2 surfaces after first task close; Tier 3 available via --full flag. Commands exist at all tiers; only help surface and suggested-next-action prompts are gated

### Definition of Done
- [ ] All ACs checked by Auditor
- [ ] `arch check` passes