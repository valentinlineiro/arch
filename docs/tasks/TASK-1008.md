## TASK-1008: Fix arch status alerts: parse INBOX sections correctly, filter advisory noise
**Meta:** P2 | XS | REVIEW | Focus:yes | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/status-command.ts
**Actor:** unknown
**Locked-commit:** 76afec39
**Created-at:** 2026-05-25T16:26:31.312Z

**Depends:** none

### Context

`arch status` Alerts section shows raw markdown from INBOX (`**Title:**`, stale escalation entries) because it line-matches rather than section-parses. Source: IDEA-status-alerts-fix.


### Relevant Context
_confidence: 0.52_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/models/reconciliation.ts _(core)_

**ADRs:**
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_
- ADR-029: Human-Centric CLI Surface & Command Dispatcher _(enforced)_

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Acceptance Criteria

- [x] `status-command.ts` parses INBOX.md sections. Alerts only read from lines in `## Alerts` section, not full file.
  - `file: cli/src/main/ts/application/commands/status-command.ts`

- [x] Only lines starting with `[ANDON_HALT]`, `[PATTERN-ALERT]`, `[CORPUS_ALERT]` shown. Advisory entries (`[ADVISORY]`) and markdown formatting lines skipped.
  - `prose: arch status output shows clean alert lines only — no **Title:** or markdown noise`

- [x] Max 3 alerts shown. If more: "... N more — run arch govern inbox".
  - `prose: verified with 5+ alerts in INBOX — shows 3 + summary line`

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Straightforward section-parse fix. Extracted `parseInboxAlerts()` as a pure exported function for testability. TDD cycle completed: 7 tests RED → GREEN. No deviations from spec.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None required.
