## TASK-1033: extract command routing from command-dispatcher.ts into command-registry.ts
**Meta:** P2 | S | REVIEW | Focus:no | 2-code-generation | local | cli/src/main/ts/application/command-dispatcher.ts, cli/src/main/ts/domain/services/command-registry.ts

**Depends:** TASK-1039

### Context

`command-dispatcher.ts` (233 lines) contains a `resolveCommand` method with nested switches and if-chains for routing. `command-registry.ts` already exists as the canonical registry but is not used for routing — the dispatcher reimplements routing inline. TASK-1039 must complete first: `command-registry.test.ts` is currently failing because the registry is missing reflect and memory namespace entries — this task cannot assume the registry is correct until those failures are fixed. This creates a single-purpose class (registry) that doesn't fulfill its purpose, and a dispatcher that's doing registry work. The fix is to make `CommandRegistry` the authoritative router and have the dispatcher delegate to it. No new abstraction needed — the registry already exists.

### Acceptance Criteria

- [x] `command-dispatcher.ts` resolves commands by delegating to `CommandRegistry` rather than via inline switch/if chains.
  - `file: cli/src/main/ts/application/command-dispatcher.ts`
  - `prose: resolveCommand contains no switch or if-else chains — it calls resolveRoute() then looks up a flat builders map`

- [x] `CommandRegistry` registers all commands and handles routing, including unknown-command fallback.
  - `file: cli/src/main/ts/domain/services/command-registry.ts`

- [x] `command-dispatcher.ts` line count is reduced by at least 40 lines.
  - `cmd: wc -l cli/src/main/ts/application/command-dispatcher.ts` returns 193 (< 195)

- [x] `npm test` passes.
  - `prose: 643/643 tests pass`

- [x] `arch review` passes.
  - `cmd: arch check passes`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H1
**Category:** [TypeHack]
**Decision:** Replaced 100-line resolveCommand nested switch/if chains with route delegation to CommandRegistry.resolveRoute() + flat builders map. Line count 233→193 (-40). One pre-existing `as any` cast carried over from original compact-escalations handler (inline command object without proper interface). 8 console.log calls in CLI command context (showHelp + trace/ask IO adapters) — appropriate for CLI commands, not debt.
**Constraint:** Pre-existing as any cast on compact-escalations handler was present in original code.
**Cost:** ~20 min.
**Forward Action:** None. [TASK-1033]
