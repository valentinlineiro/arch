## TASK-1001: arch resume: automate ANDON_HALT recovery paths
**Meta:** P2 | S | READY | Focus:yes | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/resume-command.ts, .arch/escalations.jsonl, cli/src/main/ts/application/command-dispatcher.ts

**Depends:** none

### Context

`arch resume` does not exist. Every ANDON_HALT costs 5–15 min of manual recovery and under pressure humans bypass with `--force`, corrupting the integrity audit trail. Six deterministic halt→recovery mappings exist. Source: IDEA-arch-resume (P2 S, all constraint axes pass).

### Acceptance Criteria

- [ ] `arch resume <taskId>` reads `.arch/escalations.jsonl` for the most recent OPEN ANDON record for the task, determines halt reason, executes deterministic recovery.
  - `file: cli/src/main/ts/application/commands/resume-command.ts`

- [ ] HALT→recovery table implemented (stub with error message if recovery action not yet built):
  | Reason | Recovery |
  |---|---|
  | `REVIEW_FAIL` | Resets task to READY, clears REVIEW lock, appends REVIEW_RESET to ledger |
  | `INTEGRITY_BREACH` | Runs append-only repair, commits fix, re-validates |
  | `FOCUS_VIOLATION` | Runs one `arch govern` tick |
  | `BUDGET_EXHAUSTED` | Prompts for new size, updates Meta line |
  | `ANDON_HALT` (corpus) | Runs `arch corpus audit` and surfaces output |
  | Unknown | Prints halt context, exits non-zero |
  - `prose: verified by triggering FOCUS_VIOLATION halt and running arch resume`

- [ ] After successful recovery: closes escalation record (`status: RESOLVED`, `resolved_by: arch-resume`), appends `FOCUS_RECOVERED` to `.arch/focus-ledger.jsonl`, runs `arch check`.
  - `prose: verified by checking escalations.jsonl and focus-ledger after resume`

- [ ] `arch resume` registered in command dispatcher.
  - `file: cli/src/main/ts/application/command-dispatcher.ts`

- [ ] `npm test` passes.
  - `prose: 544+ tests pass`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
