## TASK-1010: arch init: minimal project bootstrap for external repos
**Meta:** P1 | M | IN_PROGRESS | Focus:no | 2-code-generation | claude-code | cli/src/main/ts/application/commands/, arch.config.json
**Actor:** unknown
**Locked-commit:** 71ca37b9
**Depends:** none
**Created-at:** 2026-05-25T00:00:00.000Z

### Context
ARCH currently only works inside its own repo — paths, corpus, and protocol files are all assumed present. To be testable as a product, it must work on a fresh external repo from a single command: `arch init`.

This task implements the `arch init` command and the minimal starter corpus it generates.


### Relevant Context
_confidence: 0.43_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/config.ts _(core)_
- cli/src/main/ts/domain/task.ts _(core)_

**ADRs:**
- ADR-004: Flat docs/tasks/ directory with Focus field replaces sprint/backlog split _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-021: Refinement funnel TTL and admission gate _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_
- Recursive review violation tasks*(Sprint 4)*: TASK-112, 113, and 114 show a pattern where a task is created to fix a review violation, but then marked DONE with its own violations (e.g. pending ACs), leading to a chain of cleanup tasks. **Proposal:** Implement pre-archive guards (TASK-115) and enforce AC validation during `arch check`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Gaps
- `--dry-run` flag is missing from the existing `InitCommand`
- Seed task (`TASK-001`) title and walkthrough content do not match AC spec
- No tests for `InitCommand` exist yet

### Acceptance Criteria
- [ ] `arch init` command exists and is wired into the CLI dispatcher
  - `file: cli/src/main/ts/application/commands/`
- [ ] Running `arch init` in an empty directory creates exactly: `arch.config.json` (minimal), `docs/tasks/` directory, `docs/INBOX.md`, and one READY starter task (`TASK-001: Complete your first governed task`)
  - `cmd: arch init --dry-run; exit: 0`
- [ ] The generated `arch.config.json` is valid and passes `arch review` on the new repo
  - `cmd: arch review; exit: 0`
- [ ] The starter task walks the user through `arch task start → arch review → arch task done` in its description
  - `file: docs/tasks/TASK-001.md`
- [ ] `arch init` is idempotent: running it twice does not overwrite existing files
- [ ] `npm test` passes
  - `cmd: npm test --prefix cli; exit: 0`

### Definition of Done
- [ ] All ACs verified.
- [ ] `arch review` passes.

## Hansei
