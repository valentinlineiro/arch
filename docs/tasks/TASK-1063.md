## TASK-1063: capture bug: bumpVersionOnSprintClose only commits cli/packa
**Meta:** P3 | S | READY | Focus:no | 2-code-generation | local | docs/tasks/
**Depends:** none

### Acceptance Criteria
- [ ] Implementation file exists at declared context path
  - `file: (path)`
- [ ] Tests pass
  - `cmd: npm test; exit: 0`
- [ ] `arch review` passes
  - `cmd: node cli/dist/index.js review`

### Context

### Relevant Context
_confidence: 0.45_

**Files:**
- cli/src/main/ts/domain/models/sprint.ts _(core)_
- cli/src/main/ts/domain/models/config.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/application/use-cases/select-next-task.ts _(domain)_
- cli/src/main/ts/application/use-cases/build-index.ts _(domain)_

**ADRs:**
- ADR-025: Two-Track Versioning Architecture _(enforced)_
- ADR-007: Census Context Budget Check in DriftChecker _(enforced)_
- ADR-017: Deterministic Observability & Operational Metrics _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Stealth Merge Commits*(Sprint 6)*: Implicit merges from `git pull` violated the "No-Merge" policy. `arch review` initially failed to block them effectively due to a bug in `MergeCommitCheck` (it found the last 20 merges instead of checking the last 20 commits). **Resolved:** Fixed `MergeCommitCheck` in `cli/src/main/ts/infrastructure/cli/git-cli.ts` and hardened protocol. _(docs/KAIZEN-LOG.md)_
- `arch --version` does not exist as a subcommand*(Sprint 3)*: The drift checker compares versions by reading package.json directly because the CLI does not implement `--version`. This is a functional workaround but inconsistent with standard CLI conventions. *(Resolved by TASK-072)* _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

#### Intent
capture bug: bumpVersionOnSprintClose only commits cli/package.json and arch.config.json — doc comment headers (AGENTS.md, GEMINI.md, DO.md, THINK.md, ONBOARDING.html, index.html) still reference the old version string, leaving the workspace dirty and blocking the next push until manually fixed. Fix: after bumping the version, find all files that DocVersion checks, replace the old version string with the new one, and include them in the same version-bump commit.

### Definition of Done
- [ ] All ACs checked by Auditor
- [ ] `arch review` passes