## TASK-1047: add npm run test:watch for development
**Meta:** P3 | XS | READY | Focus:no | 3-chore | local | cli/package.json

**Depends:** none

### Context

`npm test` runs all 69 files serially with no watch mode. During active development, re-running the full suite manually after each change adds friction. Node's built-in test runner supports `--watch` natively since Node 18 — this is a one-line addition to package.json with zero new dependencies.

### Acceptance Criteria

- [ ] `"test:watch": "node --import tsx --test --watch src/test/ts/*.test.ts"` is added to scripts in `cli/package.json`.
  - `file: cli/package.json`
  - `cmd: npm run test:watch` starts the watcher without error (Ctrl+C to exit)

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
