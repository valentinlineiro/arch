## TASK-1122: max task attempts: auto-escalate to ANDON_HALT after N starts without close
**Meta:** P1 | S | READY | Focus:no | 1-code-reasoning | local | cli/src/main/ts/application/use-cases/mark-task-done.ts, cli/src/main/ts/application/use-cases/govern-system.ts, arch.config.json

**Depends:** none

### Context

A task an autonomous agent cannot complete will be started, worked on indefinitely, and never closed. The stale task check emits STALE_TASK and reclaims focus — but the agent restarts the same task next iteration. Infinite loop.

Fix: track start count per task. After governance.maxTaskAttempts (default 3) starts without a close, auto-escalate to ANDON_HALT and remove the task from the eligible focus set.

The attempt count lives in the task Meta line: `**Attempts:** N`. Govern increments it on each FOCUS_ACQUIRED. On third acquisition without close, govern writes ANDON_HALT to NOTIFICATIONS.md and sets task status to BLOCKED.

### Acceptance Criteria

- [ ] Task Meta line accepts an optional `**Attempts:** N` field, incremented by govern on each FOCUS_ACQUIRED
  - `prose: after 3 govern focus acquisitions on the same task, Attempts: 3 appears in Meta`
- [ ] When Attempts reaches governance.maxTaskAttempts (default 3), govern sets task status to BLOCKED and writes [ANDON_HALT] TASK-XXX exceeded max attempts to NOTIFICATIONS.md
  - `prose: TASK blocked after 3 attempts appears in NOTIFICATIONS.md`
- [ ] governance.maxTaskAttempts in arch.config.json configures the threshold (default 3)
  - `prose: setting maxTaskAttempts: 5 allows 5 attempts before ANDON_HALT`
- [ ] npm test passes
  - `prose: 634+ tests pass`

### Definition of Done
- [ ] All ACs checked

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
