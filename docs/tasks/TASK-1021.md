## TASK-1021: arch init PLG onboarding flow
**Meta:** P2 | M | IN_PROGRESS | Focus:no | 2-code-generation | local | docs/tasks/
**Actor:** unknown
**Locked-commit:** dd64dc84
**Created-at:** 2026-05-25T17:25:09.056Z
**Depends:** none

### Acceptance Criteria

- [ ] `arch init` scans the repo and bootstraps a minimal `.arch/` config with defaults. No prior ARCH knowledge required.
  - `file: cli/src/main/ts/application/commands/init-command.ts`

- [ ] `arch init` runs `arch review` after bootstrap and surfaces findings in plain language (no meta-line syntax, no Focus fields, no document-type names in output).
  - `prose: arch init output includes at least one finding category in human-readable language; no "Focus:", "Meta:", "P0", or size labels visible`

- [ ] `arch init` exits with a concrete action item: "N issues found. Run \`arch fix\` to resolve them."
  - `prose: final output line contains "arch fix" as the resolution path`

- [ ] `arch fix` resolves at least one finding class end-to-end (reduces violation count by ≥1 after running).
  - `prose: running arch fix after arch init on a fresh repo reduces the finding count by at least 1`

- [ ] `arch init` completes on an ungoverned repo in under 2 minutes.
  - `prose: timed on a fresh checkout — completes in < 120s`

- [ ] `arch init` on a repo with an existing `.arch/` directory outputs a conflict notice instead of crashing.
  - `prose: arch init on a governed repo prints "Already initialized" or equivalent`

- [ ] `npm test` passes.
  - `prose: all existing tests pass`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Context

Single command scans repo, writes minimal .arch/ config with defaults, runs arch review, exits with concrete action item ("N issues found. Run arch fix to resolve them."). Requires arch fix companion. Must work in any ungoverned repo in under 2 minutes without prior ARCH knowledge. Scope: repos with no prior governance — existing-governance conflict resolution is out of scope.


### Relevant Context
_confidence: 0.46_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/audit-inference.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- .arch/chronicle.jsonl _(utility)_

**ADRs:**
- ADR-003: DISPATCH output is ephemeral — exception to ADR-001 _(enforced)_
- ADR-029: Human-Centric CLI Surface & Command Dispatcher _(enforced)_
- ADR-001: Use git as the primary state engine _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Decision Blindness (High Velocity)*(Sprint 3)*: The agent executes architectural changes (ADR) and detects bugs (TASK-061) that stay in logs or PRs without immediate human visibility. High velocity (35 tasks/48h) makes individual monitoring impossible. **Proposal:** GOVERNANCE.md contract + INBOX.md weekly dashboard + `arch inbox` agent. _(docs/KAIZEN-LOG.md)_
- Recursive review violation tasks*(Sprint 4)*: TASK-112, 113, and 114 show a pattern where a task is created to fix a review violation, but then marked DONE with its own violations (e.g. pending ACs), leading to a chain of cleanup tasks. **Proposal:** Implement pre-archive guards (TASK-115) and enforce AC validation during `arch check`. _(docs/KAIZEN-LOG.md)_

### Gaps

- Define the minimal .arch/ config surface (which fields are required vs. optional on first init)
- `arch fix` for one finding class is in-scope for this task; comprehensive fix coverage is a follow-on. Which finding class ships first must be decided at implementation start.
- "Ungoverned repo" = no `.arch/` directory. "Partially governed" = `.arch/` exists but is incomplete. Partial governance triggers the conflict notice AC; full init flow runs only on ungoverned repos.

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

#### Intent
capture arch init PLG onboarding flow: single command scans repo, writes minimal .arch/ config with defaults, runs arch review, exits with concrete action item ('N issues found. Run arch fix to resolve them.'); requires arch fix companion command; must work in any ungoverned repo in under 2 minutes without prior ARCH knowledge

### Definition of Done
- [ ] All ACs checked by Auditor
- [ ] `arch check` passes

## Hansei
**Severity:** H0
**Category:** [SpecDrift]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.