## TASK-1012: Fix escalation lifecycle gap: close-loop write at promotion + DriftChecker stale check
**Meta:** P1 | M | READY | Focus:no | 2-code-generation | claude-code | docs/agents/THINK.md, docs/agents/DO.md, cli/src/main/ts/application/use-cases/drift-checker.ts
**Depends:** none
**Created-at:** 2026-05-25T00:00:00.000Z

### Context
The escalation store accumulated 71+ phantom OPEN entries because the lifecycle has an open end: THINK appends AWAITING_PROMOTION on IDEA surfacing, but nothing appends RESOLVED at decision time. The deduplication fix (TASK-999) prevents new duplicates on append but does not close the loop. Discovered during manual backlog review — 4 sessions of cleanup work that should be automated.

Two-part fix: (A) protocol — require RESOLVED write when a Decision is written; (B) structural — DriftChecker cross-check catches any that slip through.

### Acceptance Criteria
- [ ] `docs/agents/THINK.md` Phase 1 escalation append step updated: when writing AWAITING_PROMOTION, also document that the corresponding RESOLVED must be appended when the Decision field is written
  - `file: docs/agents/THINK.md`
- [ ] `docs/agents/DO.md` updated: when a human writes `PROMOTE → TASK-XXX` or `REJECT` in an IDEA Decision field, the promoting agent appends a RESOLVED record to `.arch/escalations.jsonl`
  - `file: docs/agents/DO.md`
- [ ] `DriftChecker` has a new `StaleEscalations` check: for each OPEN AWAITING_PROMOTION escalation, verify the referenced IDEA file has an empty Decision field; if Decision is populated, emit WARN with the subject
  - `file: cli/src/main/ts/application/use-cases/drift-checker.ts`
- [ ] `arch review` emits WARN (not FAIL) for stale escalations — informational, not blocking
- [ ] `npm test` passes
  - `cmd: npm test --prefix cli; exit: 0`
- [ ] `arch review` passes
  - `cmd: arch review; exit: 0`

### Definition of Done
- [ ] All ACs verified.
- [ ] `arch review` passes.

## Hansei
