## TASK-1074: arch task capture: clean first-use surface without meta line leakage
**Meta:** P2 | S | READY | Focus:yes | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/task-command.ts

**Depends:** none

### Context

`arch task capture "<intent>"` (current behavior): echoes the full task file content including Meta line, Focus, P-level, size. A new user sees protocol internals they don't understand. The first-use surface should be: task ID, title, and next action. Nothing else. Source: 90-day productization sprint Objective 3.

### Acceptance Criteria

- [ ] `arch task capture "<intent>"` output shows exactly: task ID, title, "Run arch task start TASK-XXX to begin." No Meta line, no Focus, no P-level visible.
  - `prose: running arch task capture "fix the login bug" shows only ID, title, next action`

- [ ] Meta line is written correctly to the task file — only hidden from terminal output, not omitted.
  - `prose: docs/tasks/TASK-XXX.md contains a valid Meta line after capture`

- [ ] If intent is < 5 words and ambiguous, prompts "Can you be more specific?" before filing.
  - `prose: arch task capture "fix" prompts for clarification`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
