## TASK-1025: arch corpus import: federated corpus from external ARCH repos
**Meta:** P2 | S | REVIEW | Focus:no | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/corpus-import-command.ts, cli/src/main/ts/application/use-cases/corpus-index.ts, cli/src/main/ts/application/command-dispatcher.ts
**Actor:** unknown
**Locked-commit:** 0e01661a
**Created-at:** 2026-05-27T08:33:24.723Z

**Depends:** none

### Context

ARCH corpus is siloed per repo. Patterns from an external project (grocery optimizer, PGW) are invisible to the main corpus. `arch ask` can't surface cross-project signals. Federated import enables compounding across projects. Source: IDEA-corpus-import. Import is one-way and read-only — no state merging.


### Relevant Context
_confidence: 0.48_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/reconciliation.ts _(core)_

**ADRs:**
- ADR-004: Flat docs/tasks/ directory with Focus field replaces sprint/backlog split _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_

**Failure Patterns:**
- Stealth Merge Commits*(Sprint 6)*: Implicit merges from `git pull` violated the "No-Merge" policy. `arch review` initially failed to block them effectively due to a bug in `MergeCommitCheck` (it found the last 20 merges instead of checking the last 20 commits). **Resolved:** Fixed `MergeCommitCheck` in `cli/src/main/ts/infrastructure/cli/git-cli.ts` and hardened protocol. _(docs/KAIZEN-LOG.md)_
- Decision Blindness (High Velocity)*(Sprint 3)*: The agent executes architectural changes (ADR) and detects bugs (TASK-061) that stay in logs or PRs without immediate human visibility. High velocity (35 tasks/48h) makes individual monitoring impossible. **Proposal:** GOVERNANCE.md contract + INBOX.md weekly dashboard + `arch inbox` agent. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

### Acceptance Criteria

- [x] `arch corpus import <path>` reads another ARCH repo's `docs/archive/TASK-*.md` and `docs/adr/ADR-*.md` and merges them into the local corpus index with a `source` tag derived from the repo directory name. → file: cli/src/main/ts/application/commands/corpus-import-command.ts
- [x] `arch corpus import https://github.com/x/y` clones the repo to a temp dir, imports, then cleans up. Same outcome as path import. → prose: verified by running arch corpus import https://github.com/valentinlineiro/arch — entries tagged source:arch
- [x] `arch corpus import <path> --as <slug>` uses explicit slug instead of directory name. → prose: arch corpus import /path/to/grocery-optimizer --as grocer returns entries tagged source:grocer
- [x] Imported entries appear in corpus index with their source tag. → prose: arch corpus audit shows imported entries with source field set
- [x] `arch ask` accepts `--project <slug>` to filter query to one source. → file: cli/src/main/ts/application/commands/ask-command.ts
- [x] Import does NOT copy task files to `docs/tasks/`, does NOT merge `.arch/` state. → prose: docs/tasks/ and .arch/ unchanged after import
- [x] Registered as `arch corpus import` in command dispatcher. → file: cli/src/main/ts/application/command-dispatcher.ts
- [x] `npm test` passes. → prose: 663 tests pass
- [x] `arch review` passes. → cmd: node cli/dist/index.js review; exit: 0

### Definition of Done
- [x] All ACs checked → prose: verified above
- [x] `arch review` passes → cmd: node cli/dist/index.js review; exit: 0

## Hansei
**Severity:** H0
**Category:** [SpecDrift]
**Decision:** Implementation matched spec. CorpusEntry gained source field; mergeImported provides idempotent per-slug merge; CorpusImportCommand handles path and URL sources; AskCommand gained --project flag delegating to AskCorpus.executeProjectFiltered.
**Constraint:** No regressions introduced — all 663 tests pass. Import is read-only and does not touch docs/tasks/ or .arch/ state beyond corpus-index.json.
**Cost:** No architectural debt. The source field is optional on CorpusEntry so existing local entries remain unaffected.
**Forward Action:** No follow-up required. Future improvement could surface imported entries in arch ask output by merging index-backed results with file-backed results (IDEA scope).
