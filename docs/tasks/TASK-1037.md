## TASK-1037: decompose GovernSystem.execute() into focused use-cases
**Meta:** P3 | L | READY | Focus:no | 2-code-generation | local | cli/src/main/ts/application/use-cases/govern-system.ts

**Depends:** TASK-1032

### Context

`govern-system.ts` is 800 lines with a single `execute()` method handling: batch drain, task archival, sprint lifecycle, replenishment, inbox hygiene, fixup commits, and corpus audit. Use-cases should be thin; domain logic belongs in domain services. TASK-1032 must complete first because it removes the SubprocessRunner side-effect from govern-system.ts, clarifying the use-case's true responsibility surface before this task splits it. The target is 5-8 smaller methods or extracted use-cases, each with a clear single responsibility and an independently testable entry point.

### Acceptance Criteria

- [ ] `govern-system.ts` is refactored so that `execute()` is < 100 lines and delegates to named private methods or extracted use-cases.
  - `cmd: wc -l cli/src/main/ts/application/use-cases/govern-system.ts` returns < 300

- [ ] Each extracted concern (archival, replenishment, inbox hygiene, etc.) is identifiable as a named method or collaborator with a clear responsibility stated in its name.
  - `prose: each delegation site in execute() calls a method whose name describes what it does (e.g. archiveCompletedTasks, replenishFocus)`

- [ ] `arch govern` produces identical observable output before and after (same files archived, same focus assignments, same inbox hygiene).
  - `prose: run arch govern on a test fixture before and after — outputs match`

- [ ] `npm test` passes.
  - `prose: all existing tests pass`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
