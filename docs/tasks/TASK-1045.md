## TASK-1045: coverage thresholds in CI: fail below 60% branch coverage
**Meta:** P2 | M | READY | Focus:no | 3-chore | local | cli/package.json, .github/workflows/

**Depends:** TASK-1040, TASK-1041

### Context

Coverage reporting (TASK-1040) measures; this task enforces. Without a threshold, coverage is informational only and will drift down under sprint pressure. 60% branch coverage is a conservative starting threshold for a governance tool — it can be raised once the baseline from TASK-1040 is known. TASK-1041 (integration tests) must complete first so the threshold is set against a suite that includes real-FS tests, not just mocks. The CI configuration exists (.github/workflows/) but does not currently run tests — this task also wires up the test run.

### Acceptance Criteria

- [ ] `.github/workflows/` contains a CI job that runs `npm test` and fails the build on test failure.
  - `file: .github/workflows/` (new or updated workflow file)
  - `prose: a PR with a failing test shows a red CI check`

- [ ] `npm run test:coverage` is run in CI and fails if branch coverage drops below 60%.
  - `prose: c8 threshold config in package.json or .c8rc sets branches: 60`

- [ ] The threshold is documented with a comment explaining it is a floor, not a target.
  - `prose: config comment reads "minimum floor — raise after baseline is established from TASK-1040"`

- [ ] CI job completes in under 3 minutes on a cold runner.
  - `prose: observed CI run duration is < 3 minutes`

- [ ] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.
