## TASK-1059: govern: always emit REVIEW_REQUEST entries for REVIEW-status tasks after each tick
**Meta:** P2 | S | DONE | Focus:no | 2-code-generation | local | cli/src/main/ts/application/use-cases/govern-system.ts
**Closed-at:** 2026-05-27T14:15:00.000Z
**Actor:** unknown
**Created-at:** 2026-05-27T12:00:00.000Z

**Depends:** none

### Context

Promoted from IDEA-inbox-stable-ledger-invariant.

`arch govern`'s INBOX regeneration pass overwrites the full INBOX, discarding manually-written REVIEW_REQUEST entries for tasks still in REVIEW status. The task file retains its REVIEW status, but the INBOX signal for the human Auditor is silently lost. The next govern tick produces an INBOX with no pending review entries, even though tasks are waiting.

The fix: after the hygiene pass, govern scans `docs/tasks/` for all REVIEW-status tasks and ensures each has a corresponding `## [AWAITING_REVIEW] TASK-XXX` section in INBOX. This is idempotent — if the entry already exists (correct format), it is not duplicated. Missing entries are appended.

This is complementary to TASK-1058 (hygiene removes stale entries for archived tasks). Together they make INBOX complete and accurate after every tick.

### Acceptance Criteria

- [x] After a govern tick, every task in REVIEW status has a corresponding `## [AWAITING_REVIEW] TASK-XXX` entry in INBOX.md. → file: cli/src/main/ts/application/use-cases/govern-system.ts
- [x] If the entry already exists, a second govern tick does not duplicate it. → prose: unit test: two consecutive ticks produce exactly one AWAITING_REVIEW entry per REVIEW-status task
- [x] A task that moves from REVIEW to DONE (archived) has its AWAITING_REVIEW entry removed on the next tick (existing hygiene via TASK-1058 covers this). → prose: confirmed by inbox-hygiene.test.ts existing tests
- [x] `npm test` passes. → cmd: npm --prefix cli test; exit: 0
- [x] `arch review` passes. → cmd: node cli/dist/index.js review; exit: 0

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Implementation complete. `emitAwaitingReviewEntries()` private method added to GovernSystem, called as step 7.1 after the hygiene pass. Idempotency via `inbox.includes(marker)` string check — simple and sufficient since the marker `## [AWAITING_REVIEW] TASK-XXX` is unique per task. Three unit tests: emit on first tick, no duplicate on second tick, no emission when no REVIEW tasks. Hygiene removal confirmed by existing inbox-hygiene tests.
**Constraint:** None.
**Cost:** One `getActive()` call and one optional `writeFile` per govern tick — negligible.
**Forward Action:** None.
