## TASK-1051: capture arch init writes hardcoded v0.6.0 into arch.config.j
**Meta:** P3 | S | REVIEW | Focus:no | 2-code-generation | local | docs/tasks/
**Actor:** unknown
**Created-at:** 2026-05-26T15:07:32.662Z
**Depends:** none

### Acceptance Criteria
- [x] `arch init` writes the running CLI version into `arch.config.json` (not hardcoded `0.6.0`).
  - `file: cli/src/main/ts/application/commands/init-command.ts`
  - `cmd: grep '"version"' smartcart-os/arch.config.json` returns `1.2.0`
- [x] `npm test` passes.
  - `cmd: npm test` — 647/647 pass
- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Context

### Relevant Context
_confidence: 0.54_

**Files:**
- cli/src/main/ts/domain/models/config.ts _(core)_
- cli/src/main/ts/domain/services/path-resolver.ts _(core)_
- cli/src/main/ts/domain/services/llm-provider.ts _(core)_
- cli/src/main/ts/domain/repositories/file-system.ts _(core)_
- cli/src/main/ts/domain/models/audit-inference.ts _(core)_

**ADRs:**
- ADR-011: Unified Provider Strategies _(enforced)_
- ADR-025: Two-Track Versioning Architecture _(enforced)_
- ADR-007: Census Context Budget Check in DriftChecker _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- `arch --version` does not exist as a subcommand*(Sprint 3)*: The drift checker compares versions by reading package.json directly because the CLI does not implement `--version`. This is a functional workaround but inconsistent with standard CLI conventions. *(Resolved by TASK-072)* _(docs/KAIZEN-LOG.md)_
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

#### Intent
capture arch init writes hardcoded v0.6.0 into arch.config.json instead of the running CLI version

### Definition of Done
- [ ] All ACs checked by Auditor
- [ ] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [AuditGap]
**Decision:** InitCommand constructor received no cliVersion; archConfig() had version:'0.6.0' hardcoded. Added cliVersion parameter to InitCommand constructor (default '0.0.0'), threaded it from CommandDispatcher, replaced hardcoded string with this.cliVersion. Verified on smartcart-os: arch.config.json now shows v1.2.0.
**Constraint:** None — single-call site in dispatcher, no interface changes required.
**Cost:** 3 files, 4 lines changed. 647/647 tests pass.
**Forward Action:** None. [TASK-1051]