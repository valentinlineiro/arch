# DISPATCH.md
<!-- Generated by CONDUCTOR — current system state -->
<!-- Overwritten on every CONDUCTOR invocation -->

Generated: 2026-04-23T00:00:00Z
Sprint: 1 | Health: 🟢 GREEN

---

## Immediate actions (human required)

- [ ] **Execute TASK-001 — Publish repo to GitHub as public OSS** — P0, human-only.
      TASK-004, TASK-006, and TASK-008 are gated on repo being live.
      Replace `[your-handle]` in `scripts/arch-install.sh` and `scripts/arch-init.sh` before pushing.
      **Urgency: today**

---

## Agent invocations (ready to run)

1. `claude` EXEC TASK-002 — context: `agents/EXEC.md` + TASK-002 entry + `docs/adr/ADR-000-template.md`
   → Write ADR-001: git as operating system. No dependencies. Unblocks TASK-003.

2. `claude` EXEC TASK-005 — context: `agents/EXEC.md` + TASK-005 entry + all `docs/agents/*.md`
   → Token audit: measure real cost per mode. No dependencies.

3. `claude` EXEC TASK-003 — context: `agents/EXEC.md` + TASK-003 entry + `docs/adr/ADR-000-template.md` + `docs/adr/ADR-001-git-as-operating-system.md`
   → Write ADR-002: context as budget. **Run only after TASK-002 PR is merged.**

4. `codex` EXEC TASK-004 — context: `agents/EXEC.md` + TASK-004 entry + `scripts/arch-install.sh`
   → Build npx arch-init. **Run only after TASK-001 is complete (repo live).**

---

## Maintenance actions

- Sync BACKLOG.md status fields: all 5 sprint tasks still show `BACKLOG` — should reflect `READY | Sprint 1` to match SPRINT.md.

---

## Flags for next planning session

- `docs/REFINEMENT.md` not evaluated — outside CONDUCTOR context budget. Confirm whether any DRAFT items are pending.
- Sprint 1 has no velocity baseline. Flag any estimate misses at sprint close for RETRO calibration.
- TASK-006 (landing page) and TASK-008 (README examples) are P2 in BACKLOG — candidates for Sprint 2 if Sprint 1 runs tight.
