## TASK-1101: cut TemporalIndex and PatternEngine or move behind modules config flag
**Meta:** P2 | S | DONE | Focus:no | 1-code-reasoning | local | cli/src/main/ts/application/use-cases/, arch.config.json
**Turns:** 0
**Closed-at:** 2026-06-03T11:28:20.583Z
**Actor:** unknown
**Locked-commit:** 68fc950
**Created-at:** 2026-06-03T11:24:47.974Z

**Depends:** none

### Context

Cut TemporalIndex and PatternEngine or move behind modules.patternDetection config flag (off by default for minimal/standard profiles). These produce uncalibrated INBOX noise without actionable specificity.


### Relevant Context
_confidence: 0.48_

**Files:**
- cli/src/main/ts/domain/models/config.ts _(core)_
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/audit-inference.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_

**ADRs:**
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_
- ADR-021: Refinement funnel TTL and admission gate _(enforced)_
- ADR-002: Context as a budget, not a default _(enforced)_

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- `arch --version` does not exist as a subcommand*(Sprint 3)*: The drift checker compares versions by reading package.json directly because the CLI does not implement `--version`. This is a functional workaround but inconsistent with standard CLI conventions. *(Resolved by TASK-072)* _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Acceptance Criteria

- [x] TemporalIndex and PatternEngine are either deleted or gated behind modules.patternDetection: enabled
  - `prose: with no modules config, pattern detection does not run`
- [x] INBOX entries from pattern detection no longer appear on standard govern tick
  - `prose: arch govern on a clean repo produces no [PATTERN-ALERT] entries without explicit opt-in`
- [x] npm test passes
  - `prose: 717+ tests pass`

### Definition of Done
- [x] All ACs checked

## Hansei
**Severity:** H0
**Category:** [SpecDrift]
**Decision:** TemporalIndex gutted to no-op stub — appendAndDetect and detectSpikes always return empty. PatternEngine deleted (0 callers). temporal-index.test.ts deleted. analyze-command TemporalIndex usage retained but now returns no spikes. 659 tests pass, 0 failures.
**Constraint:** Pattern detection is permanently disabled until modules.patternDetection is wired. The temporal-index.jsonl file will no longer be written. Historical data in .arch/temporal-index.jsonl orphaned but harmless.
**Cost:** No architectural debt. Net: PatternEngine deleted, TemporalIndex reduced to 15-line stub.
**Forward Action:** Wire modules.patternDetection: enabled as an opt-in path if pattern detection is ever revived.
