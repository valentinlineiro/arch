## TASK-1036: decompose DriftChecker into 2-3 checker groups
**Meta:** P3 | L | DONE | Focus:no | 2-code-generation | local | cli/src/main/ts/application/use-cases/drift-checker.ts
**Turns:** 0
**Closed-at:** 2026-06-01T22:31:56.497Z
**Actor:** unknown
**Locked-commit:** fdb58c4
**Created-at:** 2026-06-01T19:58:01.061Z

**Depends:** none

### Context


### Relevant Context
_confidence: 0.48_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/services/deterministic-hansei-checker.ts _(core)_

**ADRs:**
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-007: Census Context Budget Check in DriftChecker _(enforced)_
- ADR-004: Flat docs/tasks/ directory with Focus field replaces sprint/backlog split _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Stealth Merge Commits*(Sprint 6)*: Implicit merges from `git pull` violated the "No-Merge" policy. `arch review` initially failed to block them effectively due to a bug in `MergeCommitCheck` (it found the last 20 merges instead of checking the last 20 commits). **Resolved:** Fixed `MergeCommitCheck` in `cli/src/main/ts/infrastructure/cli/git-cli.ts` and hardened protocol. _(docs/KAIZEN-LOG.md)_
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Gaps
None identified. TASK-1029 is archived (dependency satisfied). drift-checker.ts is at ~870 lines and is independently audited in CODE-QUALITY-AUDIT.md.

`drift-checker.ts` is 1,396 lines with 25+ check methods spanning unrelated concerns: version drift, merge commits, census budget, hansei presence, archive integrity, priority drift, stale tasks, and more. The right decomposition is not 10 separate checkers (produces a fan-out import graph) but 2-3 bounded groups: (1) file integrity checks (census, archive, dead paths, stale tasks), (2) task lifecycle checks (hansei, priority drift, focus alignment, depends graph), (3) structural policy checks (merge commits, version, worktree, sentinel). Each group becomes a collaborator injected into DriftChecker, which becomes a thin orchestrator. TASK-1029 touches drift-checker.ts to add the maxTopLevelFiles check — it must complete before this task begins to avoid collision.

### Acceptance Criteria

- [x] `drift-checker.ts` is refactored into a thin orchestrator (target: < 200 lines) that delegates to 2-3 collaborator checkers.
  - `cmd: wc -l cli/src/main/ts/application/use-cases/drift-checker.ts` returns < 200

- [x] Each collaborator checker is in its own file in `application/use-cases/checkers/` or equivalent subdirectory.
  - `prose: find cli/src/main/ts -name "*-checker.ts" | wc -l` returns >= 3 (orchestrator + 2 groups)

- [x] All existing check methods are preserved — no check is dropped or renamed in its external behavior.
  - `prose: arch review output is identical before and after (same passing checks, same warning categories)`

- [x] Each collaborator checker is independently testable (can be instantiated without the full DriftChecker graph).
  - `prose: at least one new test file exercises a collaborator checker in isolation`

- [x] `npm test` passes.
  - `prose: all existing tests pass`

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** DriftChecker decomposed into 4 files. Orchestrator (drift-checker.ts): 78 lines, delegates to TaskHealthChecker (13 methods), StructuralChecker (11 methods), GovernanceChecker (10 methods + runExcisionGates). checkCensus retained in orchestrator (spans all domains). All 34 original check methods preserved. checkers/ subdirectory created. 707 tests pass.
**Constraint:** Each sub-checker has a copy of getMarkdownFiles() helper — minor duplication. A shared BaseChecker class would reduce this but adds inheritance complexity. Acceptable for now.
**Cost:** Three additional files in checkers/. Build time unchanged. No runtime overhead.
**Forward Action:** Consider BaseChecker with getMarkdownFiles() if more helper duplication appears.
