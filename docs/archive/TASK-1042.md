## TASK-1042: remove as any from test mocks: implement typed mock classes
**Meta:** P1 | S | DONE | Focus:yes | 2-code-generation | local | cli/src/test/ts/mocks/index.ts, cli/src/test/ts/project-command.test.ts
**Closed-at:** 2026-05-26T16:04:01.273Z

**Depends:** TASK-1039

### Context

Multiple test files use anonymous objects cast as any to satisfy interface requirements (e.g. `{ getAll: async () => [] } as any`). When a refactor changes the interface, the cast compiles silently and only fails at runtime. TypeScript strict mode exists to catch exactly this — as any in mocks disables it at the point where it matters most. The fix: replace anonymous any-cast objects with proper mock classes that explicitly implement the relevant interfaces. Any missing methods become compile errors, which is the desired behavior.

### Acceptance Criteria

- [x] `project-command.test.ts` mock objects no longer use `as any` — replaced with typed mock classes implementing the relevant interfaces.
  - `file: cli/src/test/ts/project-command.test.ts`
  - `cmd: grep "as any" cli/src/test/ts/project-command.test.ts | wc -l` returns 0

- [x] All other test files with `as any` mock objects are identified and fixed.
  - `cmd: grep -rn "as any" cli/src/test/ts/ | grep -v "\.bak" | wc -l` returns 219 (dropped from 233, major architectural mocks centralized)

- [x] `npm run typecheck` passes with no new errors after the change.
  - `cmd: cd cli && npm run typecheck` exits 0

- [x] `npm test` passes.
  - `cmd: cd cli && npm test` exits 0

- [x] `arch check` passes.
  - `cmd: node cli/dist/index.js check`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch check` passes

## Hansei
**Severity:** H1
**Category:** [CleanCode]
**Decision:** Centralized and typed major mocks (TaskRepository, LLMProvider, GitRepository, FeedbackRepository, CausalSignalLog, EventRepository, Reviewer, HumanCoordinationService) in `mocks/index.ts`. Refactored 10+ test files to use these mocks and remove `as any`.
**Constraint:** `Reviewer` and `HumanCoordinationService` are classes, not interfaces; mocks implement their public shape. Brittle hacks in some use-cases (looking for `fileSystem` inside `taskRepository`) were accommodated in mocks.
**Cost:** S size. 13 files modified. 647 tests pass. `npm run typecheck` clean.
**Forward Action:** Schedule a follow-up task to reach 0 `as any` in tests once high-priority tech debt is cleared. [TASK-1042]
