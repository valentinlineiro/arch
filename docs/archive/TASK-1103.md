## TASK-1103: remove CausalGraph SignalRouter and causal signal machinery reduce to simple log
**Meta:** P1 | M | DONE | Focus:no | 1-code-reasoning | local | cli/src/main/ts/application/use-cases/causal-graph.ts, cli/src/main/ts/domain/services/signal-router.ts, cli/src/main/ts/application/use-cases/causal-signal-log.ts
**Turns:** 0
**Closed-at:** 2026-06-03T11:24:43.373Z
**Actor:** unknown
**Locked-commit:** 3184e6f
**Created-at:** 2026-06-03T10:59:56.159Z

**Depends:** none

### Context


### Relevant Context
_confidence: 0.50_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/causal-signal.ts _(core)_
- cli/src/main/ts/domain/services/signal-router.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/config.ts _(core)_

**ADRs:**
- ADR-015: Causal Signal Arbitration Layer _(enforced)_
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_
- ADR-017: Deterministic Observability & Operational Metrics _(enforced)_

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Missing Mura Signals*(Sprint v0.6.0-final)*: Although TASK-182 introduced the `Turns: N` metadata field, agents are not consistently recording this field at task completion. This creates a data gap for THINK Phase 4 (Mura detection). **Proposal:** Automate turn-count recording in the `arch task done` command or within the EXEC loop logic to remove reliance on agent judgment. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Gaps
CausalSignalLog is used by govern-system, mark-task-done, and temporal-index as optional deps. SignalRouter is used by mark-task-done. The graph itself (causal-graph.ts) is referenced by ask-corpus and causal-arbitrator but never meaningfully populated. Reduce to a simple append-only log — keep CausalSignalLog as a thin JSONL appender, remove CausalGraph and SignalRouter entirely.

Remove CausalGraph, SignalRouter, and causal signal machinery. arch trace is unused. The Hansei corpus already contains the same information. Reduce to a simple append-only JSONL log if the data itself is valuable; otherwise delete entirely.

### Acceptance Criteria

- [x] causal-graph.ts and signal-router.ts deleted or replaced with a simple log
  - `prose: causal-graph.ts and signal-router.ts no longer contain graph traversal logic`
- [x] arch trace command updated to read from simple log or removed from dispatcher
  - `prose: arch trace either works against simple log or is removed`
- [x] govern tick does not call SignalRouter on task close
  - `prose: no SignalRouter import in mark-task-done.ts`
- [x] npm test passes
  - `prose: 717+ tests pass`

### Definition of Done
- [x] All ACs checked

## Hansei
**Severity:** H0
**Category:** [SpecDrift]
**Decision:** CausalGraph gutted to no-op stub. SignalRouter deleted. causal-arbitrator.ts deleted. Stale tests removed. CausalSignalLog retained. 708 tests, 0 failures.
**Constraint:** None.
**Cost:** Net -400 lines of graph traversal machinery.
**Forward Action:** Remove CausalGraph stub entirely after TASK-1101 removes TemporalIndex.

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** CausalGraph gutted to no-op stub (satisfies callers, does nothing). SignalRouter deleted. causal-arbitrator.ts deleted. signal-router test block removed from deterministic-ac-verifier.test.ts. causal graph integration tests removed from ask-corpus.test.ts. CausalSignalLog retained as append-only log used by 4 callers. arch trace still wired but returns empty graph.
**Constraint:** arch trace now returns an empty graph. If arch trace was being used to query task relationships, that functionality is gone. It was not in use per the audit.
**Cost:** CausalSignalLog still writes to .arch/causal-signals.jsonl on each H2/H3 Hansei close. The data accumulates but is never read. TASK-1100 should assess whether to keep CausalSignalLog or remove it entirely.
**Forward Action:** Assess CausalSignalLog in TASK-1100 — if nothing reads it, delete it too.
