## TASK-1057: arch init: seed sprint-state.json and set currentSprint on first run
**Meta:** P3 | S | DONE | Focus:no | 2-code-generation | local | cli/src/main/ts/application/commands/init-command.ts
**Actor:** unknown
**Closed-at:** 2026-05-27T11:05:00.000Z
**Created-at:** 2026-05-27T11:01:52.113Z
**Depends:** none

### Acceptance Criteria
- [x] `arch init` writes `.arch/sprint-state.json` with `status: "ACTIVE"` after first run. → file: cli/src/main/ts/application/commands/init-command.ts
- [x] `arch.config.json` generated by `arch init` has `currentSprint` set to a non-empty sprint name (format: `sprint/v{version}-{YYYY-MM}`). → prose: init-command.test.ts verifies arch.config.json currentSprint is non-empty and starts with 'sprint/'
- [x] `arch.config.json` includes `sprintCloseAfterN` and `sprintAutoNamePrefix` fields. → prose: init-command.test.ts verifies both fields present
- [x] `npm test` passes. → cmd: npm --prefix cli test; exit: 0
- [x] `arch review` passes. → cmd: node cli/dist/index.js review; exit: 0

### Context

### Relevant Context
_confidence: 0.55_

**Files:**
- cli/src/main/ts/domain/models/sprint.ts _(core)_
- cli/src/main/ts/application/use-cases/deep-analysis-state.ts _(domain)_
- cli/src/main/ts/application/use-cases/focus-ledger.ts _(domain)_
- cli/src/main/ts/application/use-cases/govern-transaction.ts _(domain)_
- cli/src/main/ts/domain/models/config.ts _(core)_

**ADRs:**
- ADR-026: Epistemic Layer Separation — Event Log, State Projection, Governance Semantics _(enforced)_
- ADR-003: DISPATCH output is ephemeral — exception to ADR-001 _(enforced)_
- ADR-004: Flat docs/tasks/ directory with Focus field replaces sprint/backlog split _(enforced)_

**Guidelines:**
- bugs.md
- core.md

**Failure Patterns:**
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_
- `arch --version` does not exist as a subcommand*(Sprint 3)*: The drift checker compares versions by reading package.json directly because the CLI does not implement `--version`. This is a functional workaround but inconsistent with standard CLI conventions. *(Resolved by TASK-072)* _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [ ] accurate — files and ADRs were on-target
- [ ] partial — correct direction, missing key files
- [ ] off — wrong files dominated

#### Intent
capture arch init does not seed sprint-state.json — first govern tick has no active sprint

### Definition of Done
- [x] All ACs checked by Auditor
- [x] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Three-part fix: initialSprintName() generates sprint/v{version}-{YYYY-MM}; archConfig() sets currentSprint, sprintCloseAfterN, sprintAutoNamePrefix; seedSprint() writes .arch/sprint-state.json with ACTIVE status and appends SPRINT_OPEN to focus-ledger. Idempotent: skips if sprint-state.json already exists. 4 new tests added to init-command.test.ts, all passing. 669 tests total, 0 failures.
**Constraint:** No regressions. seedSprint() checks fs.access before writing — safe on second run.
**Cost:** No architectural debt. Sprint state is properly seeded so govern's first tick has an ACTIVE sprint.
**Forward Action:** No follow-up required.