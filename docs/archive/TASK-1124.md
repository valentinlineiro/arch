## TASK-1124: ANDON_HALT stops the autonomous loop: exit non-zero when detected
**Meta:** P1 | S | DONE | Focus:no | 1-code-reasoning | local | cli/src/main/ts/application/commands/govern-command.ts, cli/src/main/ts/application/use-cases/govern-system.ts
**Turns:** 2
**Closed-at:** 2026-06-07T16:50:19.248Z
**Actor:** unknown
**Locked-commit:** ab8024d
**Created-at:** 2026-06-07T15:32:20.158Z

**Depends:** TASK-1123

### Context

ANDON_HALT currently writes to NOTIFICATIONS.md and continues. The loop reads the next task and starts it. The escalation mechanism is toothless in autonomous mode.

Fix: arch govern exits non-zero when ANDON_HALT entries exist in NOTIFICATIONS.md. The loop orchestrator (GitHub Actions, cron, claude task loop) treats non-zero govern as a halt requiring human intervention.

This is the circuit breaker. Everything else (stale tasks, max attempts, broken environment) escalates to ANDON_HALT. ANDON_HALT stops the loop. A human clears it by deleting the entry from NOTIFICATIONS.md and running arch govern manually.


### Relevant Context
_confidence: 0.47_

**Files:**
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/command.ts _(core)_
- cli/src/main/ts/domain/models/config.ts _(core)_

**ADRs:**
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-007: Census Context Budget Check in DriftChecker _(enforced)_
- ADR-008: Centralize halt conditions in HALT.md _(enforced)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Acceptance Criteria

- [x] arch govern exits with code 2 when NOTIFICATIONS.md contains an unresolved [ANDON_HALT] entry
  - `cmd: echo "[ANDON_HALT] test" >> docs/NOTIFICATIONS.md && node cli/dist/index.js govern; echo "exit: $?"`
- [x] arch govern prints a clear halt message naming the ANDON_HALT entry before exiting
  - `prose: ANDON_HALT: TASK-XXX exceeded max attempts — remove from NOTIFICATIONS.md to resume`
- [x] arch govern exit 0 when no ANDON_HALT entries exist (normal operation unchanged)
  - `prose: arch govern on a clean repo exits 0 as before`
- [x] arch govern --force bypasses the ANDON_HALT check for human-initiated runs
  - `prose: arch govern --force runs normally even with ANDON_HALT entries`
- [x] npm test passes
  - `prose: 634+ tests pass`

### Definition of Done
- [x] All ACs checked

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** govern-command.ts checks NOTIFICATIONS.md for [ANDON_HALT] entries at tick start. If found: prints halt message with entry summaries, exits code 2. --force flag bypasses the check for human sessions. Tested: injected [ANDON_HALT] entry, govern exited 2 with clear message. Removed entry, --force runs normally. 634 tests pass.
**Constraint:** The check reads NOTIFICATIONS.md via fileSystem.readFile — if the file is absent, govern proceeds (no-op). ANDON_HALT entries written by govern (via appendInbox routing) now correctly stop subsequent govern ticks.
**Cost:** One readFile per govern tick. Negligible.
**Forward Action:** Wire exit code 2 into GitHub Actions workflow condition so CI auto-halts on ANDON_HALT.
