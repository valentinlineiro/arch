## TASK-1073: arch triage: resolve AWAITING_TRIAGE staged IDEAs from remote corpus
**Meta:** P2 | S | DONE | Focus:no | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/triage-command.ts, cli/src/main/ts/application/command-dispatcher.ts, .arch/incoming-ideas/
**Turns:** 0
**Closed-at:** 2026-05-31T11:14:50.594Z
**Actor:** unknown
**Locked-commit:** ff2cfa2
**Created-at:** 2026-05-31T11:13:26.567Z

**Depends:** TASK-1072

### Context

TASK-1072 wired incoming remote IDEAs to `.arch/incoming-ideas/` and INBOX `[AWAITING_TRIAGE]` entries. But there is no command to resolve them. A human currently has to: read the staged file, decide PROMOTE/NOISE/DEFER, manually delete the file from `.arch/incoming-ideas/`, and manually remove the INBOX entry. That is four error-prone steps. `arch triage` collapses them into one interactive flow.


### Relevant Context
_confidence: 0.46_

**Files:**
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/application/use-cases/corpus-index.ts _(domain)_
- cli/src/main/ts/domain/models/decision.ts _(core)_

**ADRs:**
- ADR-021: Refinement funnel TTL and admission gate _(enforced)_
- ADR-004: Flat docs/tasks/ directory with Focus field replaces sprint/backlog split _(enforced)_
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_

**Failure Patterns:**
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_
- Decision Blindness (High Velocity)*(Sprint 3)*: The agent executes architectural changes (ADR) and detects bugs (TASK-061) that stay in logs or PRs without immediate human visibility. High velocity (35 tasks/48h) makes individual monitoring impossible. **Proposal:** GOVERNANCE.md contract + INBOX.md weekly dashboard + `arch inbox` agent. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Acceptance Criteria

- [x] `arch triage` lists all staged IDEA files from `.arch/incoming-ideas/` with a one-line summary each (IDEA title + source slug + status).
  - `file: cli/src/main/ts/application/commands/triage-command.ts`

- [x] `arch triage <filename>` opens the full IDEA content and prompts for a decision: `[P]romote / [N]oise / [D]efer`.
  - `prose: running arch triage smartcart-IDEA-user-*.md shows content and prompts`

- [x] On **Promote**: copies IDEA file to `docs/refinement/<filename-without-slug-prefix>.md` (entering ARCH refinement queue), removes staged file, removes `[AWAITING_TRIAGE]` INBOX entry.
  - `prose: after Promote, file appears in docs/refinement/ and INBOX entry is removed`

- [x] On **Noise**: removes staged file and INBOX entry. No copy to refinement.
  - `prose: after Noise, staged file deleted and INBOX entry removed`

- [x] On **Defer**: leaves staged file and INBOX entry in place, appends `Deferred: YYYY-MM-DD` line to the staged IDEA file so it's visible on next triage.
  - `prose: after Defer, staged file still present with Deferred date appended`

- [x] `arch triage --all` runs triage for every staged file sequentially without re-listing.
  - `prose: arch triage --all prompts for each staged IDEA in turn`

- [x] `arch triage` with no staged IDEAs prints "No incoming IDEAs to triage." and exits 0.
  - `prose: on empty .arch/incoming-ideas/ arch triage exits cleanly`

- [x] Registered in CommandDispatcher.
  - `file: cli/src/main/ts/application/command-dispatcher.ts`

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** TriageCommand implemented. Lists staged IDEAs, prompts P/N/D/S per IDEA. Promote: strips slug prefix, copies to docs/refinement/, removes staged file and INBOX entry. Noise: deletes staged file and INBOX entry. Defer: appends Deferred date, keeps file. arch triage --all: sequential. Wired in dispatcher.
**Constraint:** Interactive readline won't work in non-TTY environments (CI). arch triage is a human-operated command — acceptable.
**Cost:** No architectural debt.
**Forward Action:** Consider adding arch triage to the default help surface once remote IDEAs are flowing regularly.
