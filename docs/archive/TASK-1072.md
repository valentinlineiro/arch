## TASK-1072: syncRemotes: pull remote IDEAs into INBOX for triage
**Meta:** P1 | S | DONE | Focus:no | 1-code-reasoning | claude-code | cli/src/main/ts/application/use-cases/corpus-index.ts, cli/src/main/ts/application/use-cases/govern-system.ts
**Turns:** 0
**Closed-at:** 2026-05-31T10:50:46.715Z
**Actor:** unknown
**Locked-commit:** f2f70bd
**Created-at:** 2026-05-31T10:44:32.584Z

**Depends:** TASK-1069

### Context

TASK-1069 wired automatic corpus federation for closed tasks (docs/archive/). IDEAs from remote repos — specifically pilot-user feedback filed via POST /api/feedback in smartcart-os (TASK-032) — are never pulled. They sit in docs/refinement/ on the remote and never surface in ARCH.

This closes TASK-035 in smartcart-os (marked EXTERNAL, waiting on ARCH).

Full loop after this task:
1. SmartCart user submits feedback → IDEA-user-*.md created in smartcart-os docs/refinement/
2. arch govern in ARCH → syncRemotes() pulls IDEA files from remote docs/refinement/
3. Staged to .arch/incoming-ideas/smartcart-IDEA-user-*.md
4. govern writes [AWAITING_TRIAGE] to docs/INBOX.md
5. Human triages 5 min/week → promoted IDEAs enter ARCH refinement queue


### Relevant Context
_confidence: 0.45_

**Files:**
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/application/use-cases/corpus-index.ts _(domain)_

**ADRs:**
- ADR-021: Refinement funnel TTL and admission gate _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-015: Causal Signal Arbitration Layer _(enforced)_

**Guidelines:**
- testing-a-change.md

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Acceptance Criteria

- [x] `syncRemotes()` in `corpus-index.ts` also reads `docs/refinement/*.md` and `docs/refinement/archive/*.md` from each remote. IDEA files with `Status: DRAFT` or `Status: PROMOTED` are staged to `.arch/incoming-ideas/<slug>-<filename>`.
  - `file: cli/src/main/ts/application/use-cases/corpus-index.ts`

- [x] Only new IDEA files are staged — already-staged or already-promoted ones are skipped (delta-only).
  - `prose: running govern twice shows 0 new IDEAs on second run`

- [x] `arch govern` detects files in `.arch/incoming-ideas/` and writes `[AWAITING_TRIAGE] <slug>:<idea-name> — remote IDEA pending review` to `docs/INBOX.md`. One entry per new IDEA. Deterministic prefix (not advisory).
  - `file: cli/src/main/ts/application/use-cases/govern-system.ts`

- [x] INBOX hygiene pass skips `[AWAITING_TRIAGE]` entries (does not age them out — they persist until manually resolved).
  - `prose: AWAITING_TRIAGE entries in INBOX survive govern hygiene pass`

- [x] `corpus.remotes` absent — no change to existing behavior.
  - `cmd: npm test --prefix cli; exit: 0`

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** syncRemotes() extended to pull IDEAs from docs/refinement/ and docs/refinement/archive/ on remote. DRAFT and PROMOTED IDEAs staged to .arch/incoming-ideas/<slug>-<filename>. govern step 4.1 calls getIncomingIdeas() and writes [AWAITING_TRIAGE] to INBOX. [AWAITING_TRIAGE] added to DETERMINISTIC_PREFIXES in inbox-hygiene so it never expires. rootPath added to CorpusIndexService constructor.
**Constraint:** IDEA staging is file-based (.arch/incoming-ideas/). Once staged, marking an IDEA as triaged requires manually deleting the staged file or the entry persisting. A future TASK should add arch triage command to clear staged IDEAs.
**Cost:** syncRemotes now pulls IDEAs on every govern tick if remotes configured. Minor extra readdir per remote per tick — negligible.
**Forward Action:** File arch triage command to mark staged IDEAs as reviewed and remove from AWAITING_TRIAGE.
