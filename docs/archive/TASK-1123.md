## TASK-1123: arch review --env: environment pre-flight check before task start
**Meta:** P1 | S | DONE | Focus:no | 1-code-reasoning | local | cli/src/main/ts/application/commands/review-command.ts
**Turns:** 0
**Closed-at:** 2026-06-07T15:31:52.100Z
**Actor:** unknown
**Locked-commit:** 3a2e8d4
**Created-at:** 2026-06-07T15:29:30.026Z

**Depends:** none

### Context

If the environment is broken (npm install failed, git conflict, test suite broken) the autonomous loop proceeds into broken state. Every task it runs will fail. Without distinguishing "task is wrong" from "environment is broken", the loop retries endlessly.

arch review --env runs before arch task start in autonomous mode. Checks:
1. git status clean (no uncommitted conflicts)
2. npm test passes (or configured test command)
3. No ANDON_HALT entries in NOTIFICATIONS.md
4. arch review passes (no blocking drift)

If any check fails, exit non-zero with a diagnostic. The loop orchestrator treats non-zero as "halt until human clears".


### Relevant Context
_confidence: 0.48_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/config.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_

**ADRs:**
- ADR-007: Census Context Budget Check in DriftChecker _(enforced)_
- ADR-008: Centralize halt conditions in HALT.md _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Acceptance Criteria

- [x] arch review --env checks: git status clean, test suite passes, no ANDON_HALT in NOTIFICATIONS.md, arch review passes
  - `cmd: node cli/dist/index.js review --env; exit: 0`
- [x] arch review --env exits non-zero with a clear diagnostic when any check fails
  - `prose: broken test suite causes arch review --env to exit 1 with "tests failing: N failures" message`
- [x] arch task start runs arch review --env automatically in autonomous mode (when ARCH_AUTONOMOUS=1 env var is set)
  - `prose: ARCH_AUTONOMOUS=1 arch task start TASK-XXX runs --env check first and aborts if it fails`
- [x] npm test passes
  - `prose: 634+ tests pass`

### Definition of Done
- [x] All ACs checked

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Not yet started.
**Constraint:** None.
**Cost:** None.
**Forward Action:** None.

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** arch review --env implemented: checks ANDON_HALT in NOTIFICATIONS.md, git merge conflicts, test suite, arch review drift. DriftChecker and PathResolver added to ReviewCommand. ARCH_AUTONOMOUS=1 arch task start auto-runs --env before starting. Test command auto-detects cli/ subdirectory for ARCH-style repos. 634 tests pass.
**Constraint:** Test detection looks for cli/package.json — may not generalise to all repo layouts. Add governance.testCommand to arch.config.json as a follow-up.
**Cost:** 4 checks, ~3-5 seconds on a typical repo. Acceptable as a pre-flight gate.
**Forward Action:** Add governance.testCommand to arch.config.json so repos can specify their test command instead of relying on auto-detection.
