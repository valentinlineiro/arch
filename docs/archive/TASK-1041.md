## TASK-1041: integration test: arch review against real temp git repo
**Meta:** P1 | M | DONE | Focus:yes | 2-code-generation | local | cli/src/test/ts/arch-check-integration.test.ts, cli/src/main/ts/index.ts, cli/src/main/ts/infrastructure/cli/git-cli.ts
**Closed-at:** 2026-05-26T14:59:09.921Z

**Depends:** TASK-1039

### Context

40/69 test files use MockFileSystem. MockFileSystem records reads and writes but does not enforce real filesystem semantics: path resolution, symlinks, permissions, git object model. For a tool that writes to .arch/, executes git commands via SubprocessRunner, and has a pre-commit hook that blocks commits, the mock-vs-reality gap is a correctness risk, not a style issue. The single highest-value test the suite doesn't have: run `arch review` against a real (temp) git repo, assert on stdout. This catches what mocks can't — cases where the real git or real FS would behave differently. One well-chosen integration test is worth more than all the describe() organization in the moderate issues list.

### Acceptance Criteria

- [x] A test file `arch-check-integration.test.ts` creates a real temp directory, initializes a real git repo, bootstraps a minimal `.arch/` corpus, runs `arch review`, and asserts on the exit code and at least 3 specific stdout lines.
  - `file: cli/src/test/ts/arch-check-integration.test.ts`

- [x] The temp directory is cleaned up after the test (success or failure) — no leaked temp dirs.
  - `prose: running the test twice leaves no residual directories in /tmp or equivalent`

- [x] The integration test detects at least one violation that MockFileSystem-based tests would miss — e.g., a real git operation (missing commit reference, untracked file detection).
  - `prose: test comment identifies which AC exercises real git behavior vs. mock behavior`

- [x] A second integration test covers `arch govern` against the same real-repo fixture — verifies at least one task is archived and focus is assigned.
  - `file: cli/src/test/ts/arch-check-integration.test.ts` (second test in same file is fine)

- [x] Integration tests are tagged or separated so `npm test` runs them but they can also be excluded via a flag if needed.
  - `prose: npm test runs both unit and integration tests; integration tests are identifiable by filename pattern`

- [x] `npm test` passes (including the new integration tests).
  - `cmd: cd cli && npm test` exits 0

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [ ] All ACs checked
- [ ] `arch review` passes

## Hansei
**Severity:** H1
**Category:** [SymbolDiscovery]
**Decision:** Implemented comprehensive integration tests in `arch-check-integration.test.ts`. Discovery: Found two critical bugs during implementation: 1) `index.ts` had a brittle relative import for `package.json` that failed when running via `tsx` from a different CWD. 2) `GitCli.getMergeCommits` used `HEAD~limit..HEAD` which fails for repositories with short history; fixed to use `-n limit`.
**Constraint:** Real git operations are significantly slower than mocked FS, but necessary for correctness validation of the integrity engine.
**Cost:** M size. 3 new tests, 2 bug fixes in core CLI logic. 647 tests pass.
**Forward Action:** None. [TASK-1041]
