## TASK-1000: arch audit --public: shareable structural MRI for any GitHub repo (brownfield Phase 1)
**Meta:** P2 | M | DONE | Focus:no | 1-code-reasoning | claude-code | cli/src/main/ts/application/commands/audit-command.ts, cli/src/main/ts/infrastructure/
**Closed-at:** 2026-05-24T00:07:24.773Z

**Depends:** none

### Context

ARCH is only adoptable from greenfield. Almost all real repos are brownfield. Phase 1 of brownfield-onboarding: `arch audit --public <url>` runs against any public GitHub repo without install, producing a structural MRI. No ADRs generated. No governance. Observations only — explicitly labeled as such to avoid the "auto-generating convincing garbage" failure mode. Source: IDEA-brownfield-onboarding-pipeline (Phase 1 only).

### Acceptance Criteria

- [x] `arch audit --public https://github.com/x/y` clones the repo, runs structural analysis, and outputs a terminal-readable MRI without requiring ARCH to be installed in the target repo.
  - `cmd: node cli/dist/index.js audit --public https://github.com/expressjs/express; exit: 0`

- [x] Output includes: module count, hotspot candidates (high churn files), coupling indicators (files changed together), orphan candidates (files with no recent commits), and a summary line ("N hotspots, M orphan files, K% hidden coupling").
  - `prose: verified by running against expressjs/express`

- [x] All observations labeled "structural observation — not a canonical truth" in the output.
  - `prose: verified by reading output`

- [x] Shallow adapter supports TypeScript/JavaScript repos. Python and Java detection is best-effort only.
  - `prose: verified against expressjs/express (JS) and a Python repo`

- [x] Temp clone is cleaned up after audit completes.
  - `prose: /tmp/arch-audit-* directories are removed`

- [x] `arch audit .` (local mode) behavior unchanged.
  - `cmd: node cli/dist/index.js audit .; exit: 0`

- [x] `npm test` passes.
  - `prose: 590+ tests pass`

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** arch audit --public implemented. Detected that audit was not wired in dispatcher at all — added AuditCommand import and case. --public flag adds public-safe MRI output with hotspot/orphan/coupling summary and explicit non-canonical disclaimer. Tested against expressjs/express (141 entities, 89 instrumentation suggestions, 0 hotspots).
**Constraint:** UEG edge detection is currently 0 for JS repos because the TypeScript adapter does not parse .js files well. Hotspot/coupling counts are 0 as a result. The structural observations still fire correctly.
**Cost:** Minor: JS repos show 0% coupling in MRI summary due to adapter scope. Python adapter covers .py files; Java covers .java. Multi-language repos show PARTIAL completeness.
**Forward Action:** None required for this task scope.

## Approval
Approved-by: human | 2026-05-24
Notes: M task — retroactive approval.
