## TASK-1063: fix: bumpVersionOnSprintClose must include doc-header version update in the bump commit
**Meta:** P2 | S | DONE | Focus:no | 2-code-generation | local | cli/src/main/ts/application/use-cases/govern-system.ts
**Closed-at:** 2026-05-27T13:45:00.000Z
**Actor:** unknown
**Created-at:** 2026-05-27T13:04:22.116Z
**Depends:** none

### Context

Observed on sprint v1.2.0 → v1.2.1 close. `bumpVersionOnSprintClose` in `govern-system.ts` commits `cli/package.json` and `arch.config.json` with the new version but does not touch the doc comment headers in `AGENTS.md`, `GEMINI.md`, `docs/AGENTS.md`, `docs/agents/DO.md`, `docs/agents/THINK.md`, `docs/ONBOARDING.html`, and `docs/index.html`. Those files embed the version string (e.g. `<!-- ARCH Framework v1.2.0 -->`). After govern runs, those files are left with the old version string while `arch.config.json` already says the new version — the workspace is dirty and the DocVersion DriftChecker blocks the next `git push` until manually patched.

Fix: after bumping the version, `bumpVersionOnSprintClose` reads the DocVersion-checked file list, replaces occurrences of the old version string with the new one (exact string match, no regex needed), stages them, and includes them in the same `chore: bump version to vX.Y.Z on sprint close` commit.

The DocVersion checker already knows which files to check — reuse that list rather than hardcoding it.

### Acceptance Criteria

- [x] `bumpVersionOnSprintClose` updates the old version string to the new one in all files the DocVersion DriftChecker inspects. → file: cli/src/main/ts/application/use-cases/govern-system.ts
- [x] After a simulated sprint close the workspace has no uncommitted changes to doc-header files. → prose: unit test: spy filesystem seeded with doc files containing old version; after bumpVersionOnSprintClose runs, all doc files contain the new version string
- [x] `npm test` passes. → cmd: npm --prefix cli test; exit: 0
- [x] `arch review` passes. → cmd: node cli/dist/index.js review; exit: 0

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H0
**Category:** [no-issue]
**Decision:** Minimal fix: `bumpVersionOnSprintClose` now iterates the same doc-file list DocVersion uses, replaces `v{old}` with `v{new}` via `replaceAll`, and stages all seven files in the same commit. Absent files are silently skipped. One new unit test verifies content update and staging for all seven files. No design deviation from the TASK spec.
**Constraint:** None.
**Cost:** None — replaceAll on small doc files is negligible.
**Forward Action:** None.
