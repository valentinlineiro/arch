## TASK-1069: automatic corpus federation on govern tick
**Meta:** P2 | M | DONE | Focus:no | 2-code-generation | local | cli/src/main/ts/infrastructure/
**Turns:** 0
**Closed-at:** 2026-05-31T09:53:19.708Z
**Actor:** unknown
**Locked-commit:** 5c0f363
**Created-at:** 2026-05-31T09:51:53.729Z

### Context

ARCH's corpus learning is project-local by default. Inter-project federation is supported (`arch corpus import`) but manual. Manual means it doesn't happen. The Hansei from pilot projects contained genuine domain learning but lived only in the local archive, invisible to other projects. The compounding value of inter-project learning was never realized.

Add `corpus.remotes` to `arch.config.json` with `syncOnGovern: true`. On each govern tick, run a delta sync: fetch remote archive manifest, download only changed Hansei digests, merge into local corpus index. Sync is non-blocking — if remote is unreachable, govern logs a warning and continues. Local archive tasks are also auto-indexed on govern without requiring manual `arch corpus import`.


### Relevant Context
_confidence: 0.45_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_
- cli/src/main/ts/domain/models/action.ts _(core)_
- cli/src/main/ts/domain/models/reflect-decision.ts _(core)_

**ADRs:**
- ADR-016: Define the semantic boundary of the domain layer _(enforced)_
- ADR-006: Depends Graph Validation in DriftChecker Domain Service _(enforced)_
- ADR-014: Causal Graph Schema for Chronicle _(enforced)_

**Guidelines:**
- testing-a-change.md
- bugs.md

**Failure Patterns:**
- Decision Blindness (High Velocity)*(Sprint 3)*: The agent executes architectural changes (ADR) and detects bugs (TASK-061) that stay in logs or PRs without immediate human visibility. High velocity (35 tasks/48h) makes individual monitoring impossible. **Proposal:** GOVERNANCE.md contract + INBOX.md weekly dashboard + `arch inbox` agent. _(docs/KAIZEN-LOG.md)_
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Gaps

None identified — scope is bounded to config schema, delta sync logic, and auto-indexing on govern.

### Acceptance Criteria

- [x] `arch govern` with `syncOnGovern: true` syncs remote archive delta at tick end  →  prose: verified by configuring a remote and observing sync log in govern output
- [x] Sync is delta-only — tasks already in local corpus are not re-fetched  →  prose: verified by running govern twice; second run shows "0 new tasks synced"
- [x] Govern completes normally if remote is unreachable (non-blocking sync)  →  prose: verified by setting an invalid remote URL; govern logs warning and exits 0
- [x] Local archive tasks are auto-indexed on govern without manual `arch corpus import`  →  prose: verified by closing a task and running arch ask — the closed task appears in results without running corpus import
- [x] `corpus.remotes` absent from config causes no change to existing govern behavior  →  cmd: npm test --prefix cli; exit: 0
- [x] `arch review` passes  →  cmd: arch review; exit: 0

### Definition of Done
- [x] All ACs checked  →  prose: all predicates above verified
- [x] `arch review` passes  →  cmd: arch review; exit: 0

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** Automatic corpus federation wired into govern tick (step 4.1). syncRemotes() added to CorpusIndexService — git clones each remote, reads docs/archive/, indexes new tasks with source tag. Delta-only: existing entries skipped. Non-blocking: remote failures log warning and continue. corpus.remotes config key added to arch.config.json with smartcart-os. 707 tests pass.
**Constraint:** Each sync does a git clone --depth 50 which is ~5-30s depending on repo size. On slow connections govern tick becomes noticeably slower. Mitigated by depth 50 and non-blocking behavior.
**Cost:** Network dependency added to govern tick. Repos without corpus.remotes are unaffected — the block is guarded by config check.
**Forward Action:** Consider caching the remote manifest hash to skip clones when remote archive has not changed. Out of scope for this task.
