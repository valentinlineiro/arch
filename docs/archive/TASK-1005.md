## TASK-1005: Decouple arch govern reflect from govern tick : enforce structural LLM/governance separation
**Meta:** P1 | S | DONE | Focus:yes | 1-code-reasoning | claude-code | cli/src/main/ts/application/use-cases/govern-system.ts, cli/src/main/ts/application/commands/reflect-command.ts
**Turns:** 0
**Closed-at:** 2026-05-24T23:14:41.478Z
**Actor:** unknown
**Locked-commit:** 4ccc4238
**Created-at:** 2026-05-24T21:57:31.867Z

**Depends:** none

### Context

ADR-026 (Epistemic Layer Separation) declares `arch govern` deterministic. But `govern-system.ts` currently calls `AuditCommand.runQuiet()` and hooks into `arch govern reflect` in the reflect path, creating a structural seam where LLM output (from `arch analyze`) can flow back into govern's execution cycle.

The principle: governance gates must be owned by deterministic code. Advisory is fine but must be structurally isolated — no path from LLM output to a governance pass/fail decision.

Currently broken:
1. `arch govern reflect` runs inside the govern tick on deep cadence — LLM advisory output flows into INBOX.md which govern reads for PATTERN-ALERT signals
2. INBOX entries from THINK are mixed with deterministic alerts — no structural distinction
3. An agent in CI reading exit codes cannot distinguish "govern halted on deterministic ANDON" from "reflect wrote a warning"


### Relevant Context
_confidence: 0.45_

**Files:**
- cli/src/main/ts/domain/models/task.ts _(core)_
- cli/src/main/ts/domain/services/command-registry.ts _(core)_
- cli/src/main/ts/domain/models/context-index.ts _(core)_
- cli/src/main/ts/domain/models/reflect-decision.ts _(core)_
- cli/src/main/ts/domain/models/decision.ts _(core)_

**ADRs:**
- ADR-012: Exec/Bridge Layer Bugfixes - maxBuffer, buildCommand signature, local routing _(enforced)_
- ADR-026: Epistemic Layer Separation — Event Log, State Projection, Governance Semantics _(enforced)_
- ADR-013: Two-Tier Drift Detection Architecture _(enforced)_

**Failure Patterns:**
- Invariant Discovery Gap*(2026-05-12)*: The boundary ambiguity between `arch govern` (deterministic enforcement) and THINK (LLM analysis) was not surfaced by any ARCH mechanism. No DriftChecker rule, no structural check, no semantic scan detected it. A human noticed it during a reflection session. The specific risk: `arch govern` triggers THINK via `runConduct()`, creating naming confusion between enforcement and advisory synthesis — with no written invariant to refuse the rationalization "THINK already participates in govern, so it can also…". **Resolution:** Constitutional split written in IDENTITY.md §7. IDEA-architectural-tension-capture proposed as a new artifact class for structural ambiguities that are not yet broken but will be misused. **The deeper pattern:** ARCH models tasks, decisions, and causal edges, but not category errors or ontological drift. Invariants that live only in the author's head do not scale. _(docs/KAIZEN-LOG.md)_
- Phantom Archive Sync Latency*(Sprint v0.6.0-final)*: Tasks marked `DONE` by an Auditor (human or agent) remain in `docs/tasks/` until the next `arch govern` tick. This creates a "stale backlog" window where `arch status` and INBOX show tasks that are technically complete. **Proposal:** Integrate phantom-archive sync directly into `arch task done`. _(docs/KAIZEN-LOG.md)_

### Context Feedback
- [x] accurate — files and ADRs were on-target
- [x] partial — correct direction, missing key files
- [x] off — wrong files dominated

### Acceptance Criteria

- [x] `arch govern reflect` is removed from the govern tick. Reflect runs only when explicitly invoked (`arch govern reflect` as a manual command or separate scheduled job). The govern tick never calls reflect or analyze.
  - `prose: verified by reading govern-system.ts — no reflect/analyze invocation`

- [x] `arch govern` tick is fully deterministic: no LLM invocations, no shell-out to advisory channels, no reading of reflect output as input.
  - `prose: verified by reading govern-system.ts execute() method — no spawnSync on any LLM binary`

- [x] INBOX.md entries from THINK/reflect are written with a `[ADVISORY]` prefix; deterministic entries (ANDON_HALT, PATTERN-ALERT, CORPUS_ALERT) retain their current prefixes. The distinction is structural, not just visual.
  - `grep: "\\[ADVISORY\\]" cli/src/main/ts/application/commands/reflect-command.ts`

- [x] `arch govern` exit code is always determined by deterministic checks only. ANDON_HALT from a deterministic check = non-zero. Advisory output = always 0 govern exit.
  - `prose: verified by running arch govern with broken reflect config — govern still exits 0`

- [x] ADR-034 filed extending ADR-026 with explicit govern/reflect separation rule.
  - `file: docs/adr/ADR-034-govern-reflect-separation.md`

- [x] `npm test` passes.
  - `prose: 544+ tests pass`

- [x] `arch review` passes.
  - `cmd: node cli/dist/index.js review`

### Definition of Done
- [x] All ACs checked
- [x] `arch review` passes

## Hansei
**Severity:** H1
**Category:** [SpecDrift]
**Decision:** govern-system.ts has zero LLM invocations — verified by grep. The advisory INBOX path (analyze-command.ts AuditCommand.runQuiet) now prefixes entries [ADVISORY][EMERGENT] with HTML comment noting advisory-only status. ADR-034 filed with explicit INBOX entry classification table. HanseiSynthesizer PATTERN-ALERT is deterministic (corpus counts) — correctly kept without advisory prefix.
**Constraint:** The [ADVISORY] prefix is a naming convention, not a code-enforced gate. An agent reading INBOX.md must filter by prefix — the wall is structural in that govern never calls LLM, but advisory entries still co-exist in the same file.
**Cost:** No architectural debt. If INBOX co-mingling becomes a problem, a future task can split docs/INBOX.md into deterministic and advisory channels.
**Forward Action:** None required.
